# Let The Server Run The Meeting One Beat At A Time

This ExecPlan is a living document. Keep `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` current as work proceeds. Maintain this document in accordance with [`PLANS.md`](../PLANS.md).

This plan governs GitHub epic [#77](https://github.com/Phazzie/The14thstep/issues/77) and its ordered issues [#81](https://github.com/Phazzie/The14thstep/issues/81), [#82](https://github.com/Phazzie/The14thstep/issues/82), and [#83](https://github.com/Phazzie/The14thstep/issues/83). Begin only after `plans/private-meeting-access-execplan.md` has established the common ownership gate for `/meeting/[id]` and its children.

## Purpose / Big Picture

After this work, the server decides the next observable moment in a meeting: who speaks, what kind of share they give, when the room displays a ritual cue, and when the room waits for the user. The browser asks for one beat, renders it, reports completion, and asks again. Refreshing in the middle of a round resumes from the saved beat instead of replaying the round and appending duplicate shares.

The meeting should still feel like a room, not a stepper. The visible opening, introductions, topic choice, three rounds, hard question, crisis interruption, closing, reflection, and farewell remain. The implementation proof is that two clients given the same saved meeting state receive the same next beat, that a repeated request returns the same beat, and that refresh never increases the transcript count by replaying completed work.

The empty chair also becomes a fresh generated room moment. It is persisted as part of the transcript, survives refresh, and follows the current prompt rules instead of repeating the literal sentence now embedded in the Svelte component.

## Progress

- [x] (2026-09-15) Addressed both PR #96 review findings: validate meeting identity and roster before active replay, require active character/crisis responder membership, and keep room-owned values out of the existing database share type. Server tests pass 275/275; type check has zero errors and eight existing warnings.

- [x] (2026-09-12 14:10Z) Read the current phase state machine, meeting page orchestration, share and user-share routes, database phase persistence, prompt builders, tests, and issues #81 through #83.
- [x] (2026-09-12 14:10Z) Confirmed that the March plan's frontend-owned speaking order conflicts with the current server-owned epic and archived that plan as historical evidence.
- [x] (2026-09-12 14:10Z) Chose a persisted active-beat contract, stable beat ids, and compare-and-set phase writes so retries and competing requests converge on one answer.
- [x] (2026-09-14 03:14Z) Integrated PR review findings for canonical completed retries, fully atomic close persistence with lease renewal, version-gated legacy cutover, durable crisis resources, deterministic roster fallback in every consumer, canonical intake and share crisis triggers, crisis and room-moment quality gates, durable generation claims, character skips, room cues, repeated round speakers, in-progress client retry, truthful room-entry labels, meeting-scoped callback effects, summary hydration, one route-and-renderer promotion boundary, compliant close-summary guidance, server-side Grok browser fixtures, injected time, milestone order, test paths, and the required real-system probe stage.
- [x] (2026-09-14 07:50Z) Closed final planning-review gaps with atomic terminal topic/pass outcomes, a character-only interaction subtype, and explicit protocol-stamp ownership in the renderer cutover slice.
- [x] (2026-09-14 08:14Z) Closed the next review pass by ensuring fallback visitors have durable UUID identities before selection, resolving callback domain ids before close checkpointing, fencing generated-share persistence with the current lease token, and expanding the prompt audit to catch worded sentence counts.
- [x] (2026-09-14 08:21Z) Made visitor persistence identities meeting-scoped after confirming the current domain id contains only the start-time second and slot, and specified a nullable unique constraint that an idempotent Supabase upsert can target directly.
- [x] (2026-09-14 08:34Z) Required complete persisted visitor narrative profiles, restricted the control ledger to share-gate passes, and added a zero-active-version-1 rollback gate before removing the new renderer.
- [x] (2026-09-14 09:02Z) Made epic #79's unique, migration-seeded core character identity a hard prerequisite for database-backed beat work and added callback lifecycle-version fencing with stale-checkpoint recomputation.
- [x] (2026-09-14 09:26Z) Closed the cutover check-to-deploy race with a database creation fence, required callback candidates to match the scanned meeting transcript, and added beat-id-only recovery for a partially persisted user share after refresh.
- [x] (2026-09-14 09:54Z) Serialized spoken and pass submissions through one user-gate outcome row, moved controlled cue persistence into the transaction that first exposes the cue beat, and gave the planned migration a unique fourteen-digit Supabase version.
- [x] (2026-09-14 10:17Z) Added deterministic close-lock ordering, a temporary protocol bridge plus inactivity expiry for abandoned meetings, and crisis-resource persistence at crisis entry before generated support.
- [x] (2026-09-14 10:45Z) Made the activity migration safe for existing rows and legacy creators, required every meeting ledger to cascade with its parent, and expanded prompt repair to omit empty sections instead of emitting placeholders.
- [x] (2026-09-14 21:35Z) In isolated offline worktree `codex/offline-meeting-beats`, completed C01 and C02: added the runtime-validated non-crisis and crisis-support beat contract, rejected room-owned values from character beats, exported the controlled crisis-resource payload, and added backward-compatible beat fields to phase state. Focused core tests passed 55/55 and `npm run check` had 0 errors with the existing 8 Svelte warnings.
- [x] (2026-09-14 23:26Z) In the same isolated worktree, completed C03: added pure opening-beat selection that returns a version-1 active beat by object identity, deterministically selects roster member `marcus` at `opening:<cursor>:character_share:marcus` with the shipped 3000ms pause, and rejects unversioned or incompletely initialized state before selection. Focused core tests passed 61/61 and `npm run check` had 0 errors with the same 8 existing Svelte warnings.
- [ ] Milestone 1: define the pure beat engine and repair the ritual prompt constraints it will use.
- [ ] Milestone 2: probe and implement versioned beat, share-analysis, and close persistence in seam order.
- [ ] Milestone 3: expose the protected next-beat protocol and prove stable retries.
- [ ] Milestone 4: make character, user, crisis, topic, and close routes complete only the active beat.
- [ ] Milestone 5: generate, validate, and persist the empty-chair moment.
- [ ] Milestone 6: replace the page's hand-written meeting script with one generic renderer loop.
- [ ] Milestone 7: prove replay-free refresh through every meeting phase.

## Surprises & Discoveries

- PR #96 review found that active replay bypassed roster validation and widening ShareInteractionType promised database writes the seam rejected. Regression coverage now checks replay with invalid rosters, absent speakers and invalid meeting IDs, plus compile-time/runtime agreement at the append-share boundary.

- Observation: the browser, not the phase machine, owns the meaningful sequence inside every phase.
  Evidence: `app/src/routes/meeting/[id]/+page.svelte` contains `runFreshMeeting`, `runRoundOne`, `runRoundTwo`, `runRoundThree`, `runClosing`, speaker selection, optional crosstalk, hard-question selection, and all phase-specific waits.

- Observation: refresh explicitly restarts a whole round.
  Evidence: `continueFromPersistedPhase` dispatches `sharing_round_1` to `runRoundOne`, `sharing_round_2` to `runRoundTwo`, and so on. `MeetingPhaseState` records the phase and who spoke, but it has no active beat or substep cursor.

- Observation: existing server routes can advance a coarse phase but cannot identify one retryable generated result.
  Evidence: `shares` has `sequence_order` but no stable beat id, while `meetings.phase_state` is updated as an unversioned JSON value. A refreshed client can therefore request new text for work that already ran.

- Observation: the existing meeting plan cannot govern this epic.
  Evidence: the archived `archive/plans/restore-virtual-recovery-meeting-execplan-2026-03-19.md` explicitly requires frontend-owned speaking order, forbids a new orchestration endpoint, and says the page is allowed to orchestrate. Issue #81 requires the opposite boundary.

- Observation: the empty-chair prompt exists but is unused, while the visible moment is hardcoded locally.
  Evidence: `buildEmptyChairPrompt` in `app/src/lib/core/prompt-templates.ts` has no imports outside its test. `runFreshMeeting` pushes `This chair stays empty for everyone who couldn't make it tonight.` on every new meeting.

- Observation: the prompt named by #83 violates the repository's current hard rule against sentence counts, and nearby ritual prompts contain the same conflict.
  Evidence: `buildEmptyChairPrompt` says `Target 2-4 spoken sentences`; the ritual opening, introduction, reading, topic, and closing builders also contain exact sentence ranges. Root `AGENTS.md` says never add `exactly N sentences` to a generation prompt. The issue's old wording is historical context, not authority over the current rule.

- Observation: the selected meeting topic is currently browser state and is sent back as generation input without being persisted through a meeting-topic seam.
  Evidence: `TOPIC_OPTIONS`, `selectedTopic`, and `chooseTopic` live in `+page.svelte`; `DatabasePort` has no operation for recording the choice. A server-owned sequence needs the chosen topic to survive refresh.

- Observation: user-share analysis currently runs on the request body before the database decides which racing request owns the beat.
  Evidence: `user-share/+server.ts` performs crisis, heavy-disclosure, and significance analysis before `addShare`. If two different payloads race for one beat, the losing request could advance phase state using analysis that does not describe the unique persisted share.

- Observation: the close route has several externally visible side effects before it reaches post-meeting state, but no durable claim or stored close response.
  Evidence: summary generation, memory extraction, `completeMeeting`, callback scanning, callback lifecycle, and phase persistence can all run again on a repeated request. The meeting contract does not expose a canonical close result that a retry can return.

- Observation: ownership does not make the expansion request's topic and recent transcript trustworthy.
  Evidence: `expand/+server.ts` verifies the target share belongs to the meeting but builds generation and quality prompts from browser-supplied `topic` and `recentShares`.

- Observation: the current ritual transition helpers read `new Date()` internally.
  Evidence: a beat completion that calls `transitionToNextPhase` would produce different `phaseStartedAt` values for identical arguments unless the server supplies time explicitly.

- Observation: the existing crisis endpoint accepts the trigger text and display name from the browser.
  Evidence: persisting only a pre-crisis phase cannot prove which user share caused support after refresh, and altered client text could drive a retry for the same support beat.

- Observation: the versioned phase, user-analysis, and close-claim operations are new database seam behavior with no captured provider evidence.
  Evidence: the repository requires a real-system probe before fixtures, mocks, contract tests, and adapter work. Existing database captures predate these columns, indexes, and contention results.

- Observation: the current SSE share route emits generated chunks before the final share row is appended.
  Evidence: two callers can both miss the beat lookup and show different accepted candidates before the unique beat index selects one stored winner. Persistence after streaming cannot retract the losing text.

- Observation: acknowledged room cues exist only in browser transcript state.
  Evidence: clearing a `room_cue` active beat leaves no stored transcript record for the moment of silence, readings, or closing ritual, so refresh cannot rehydrate identical transcript ids and content.

- Observation: a lost close response followed by refresh can reach `finished` without restoring reflection text.
  Evidence: the proposed `finished` beat stops the renderer, while the page loader does not yet map the meeting's stored summary into initial page data.

- Observation: crisis support is generated transcript content but the current crisis route has no quality-validation loop.
  Evidence: persisting the first model response would let authenticity or voice-consistency scores below 6 reach the most sensitive part of the meeting and violate the root generation constraint.

- Observation: an intake-triggered crisis has no user-share row.
  Evidence: the private-access plan derives setup crisis from the canonical stored `userMind` immediately after join, before the user has submitted a transcript entry, so a support beat that requires only `triggerShareId` cannot represent that path.

- Observation: close phase advancement and close-run completion are currently two writes.
  Evidence: a worker can clear the close beat and crash before storing the run result, leaving recovery without the old active beat unless those writes share one transaction.

- Observation: the close summary has an exact sentence range outside the ritual prompt template module.
  Evidence: `buildCloseSummaryPrompt` in `app/src/lib/core/meeting.ts` says `4-6 spoken-style sentences`, and the server-owned close beat invokes that workflow.

- Observation: route-backed browser refresh tests require the Grok seam inside the preview process.
  Evidence: `/share`, `/crisis`, `/close`, and `/room-moment` perform server-side model calls that browser interception cannot replace without bypassing the route persistence under test.

- Observation: the hard quality rule says to skip a character whose share cannot pass, but a stored share is currently the only terminal generation outcome.
  Evidence: leaving that beat active retries forever, while advancing without durable skip evidence lets refresh regenerate a character the room already skipped.

- Observation: the shipped page keeps a meeting open when participant persistence fails by deriving the same fallback roster again.
  Evidence: `+page.server.ts` seeds `selectCharacters` from the meeting id and continues on the generated seats after `saveMeetingParticipants` fails; `/next` must use that same roster or it can strand the already-rendered room.

- Observation: crosstalk may be followed by a scheduled share from the same character in one round.
  Evidence: the current page deliberately uses the second speaker for both moments, while `recordCharacterSpoke` rejects a character already present in `charactersSpokenThisRound`.

- Observation: a refreshed client can encounter a valid in-progress generation or close lease.
  Evidence: the endpoints return a retryable response with no content, but a renderer that treats it as an ordinary failure can stop even after the claim holder stores the canonical result.

- Observation: current server transcript labeling treats every null character id as the user.
  Evidence: durable `room_cue` and `empty_chair` entries also have null character ids, so close and expansion prompts would otherwise attribute ritual text to the person in the meeting.

- Observation: a route retry can arrive after its effect and phase completion both committed.
  Evidence: requiring the old active beat before reading a beat-owned share, analysis, completed run, or skip rejects the exact lost-response retry that idempotency is meant to recover.

- Observation: meeting completion fields currently precede the proposed atomic close finalizer.
  Evidence: writing `endedAt`, summary, and notable moments through `completeMeeting` before phase/run finalization can leave a durably half-closed meeting if the worker stops at that boundary.

- Observation: historical phase JSON cannot reveal a safe beat cursor for every mid-round meeting.
  Evidence: legacy shares have no beat ids, and a repeated crosstalk/scheduled speaker makes `charactersSpokenThisRound` insufficient to reconstruct which observable moments already ran.

- Observation: the crisis resource card exists only in the successful endpoint response.
  Evidence: after support text and phase completion persist, a lost response plus refresh can restore the transcript but has no durable fact telling the page to show the standard 988, 741741, and 911 resources.

- Observation: close preparation can outlive its initial lease.
  Evidence: summary, memory, fallback-summary, and callback-candidate model calls occur before checkpointing, so an expired lease without renewal permits a second healthy worker to repeat them.

- Observation: a fallback visitor beat needs the same character profile outside `/next`.
  Evidence: if participant saving fails, `/next` can select a derived visitor whose row is absent; `/share` still needs that visitor's voice data, and close/expansion need the same derived roster to label the transcript.

- Observation: topic completion changes phase but leaves no beat-keyed terminal evidence.
  Evidence: if the atomic topic/phase update commits and its HTTP response is lost, a retry sees another active beat and cannot distinguish its completed topic acknowledgment from a stale request. Listening-only or explicit passes at later share gates have the same no-transcript shape.

- Observation: expanding `ShareInteractionType` with room-only values also widens a character beat unless its contract is narrowed.
  Evidence: `room_cue` and `empty_chair` would otherwise be type-valid `character_share.interactionType` values even though their renderer and transcript ownership belong to the room.

- Observation: a derived visitor profile is not yet a durable share foreign-key target.
  Evidence: `saveMeetingParticipants` creates visitor character rows as part of the same workflow that can fail, while `appendShare` later resolves the visitor's domain id to an existing `characters.id` UUID. Returning only the fallback voice profile can therefore leave `/share` unable to persist that speaker.

- Observation: the current visitor domain id is not globally unique across meetings.
  Evidence: `visitorId(nowIso, index)` contains the start-time second and visitor slot but not the meeting id. Two meetings started in the same second can therefore produce the same domain id for different generated profiles, so a global `visitor:<domain-id>` persistence key is unsafe.

- Observation: callback discovery uses domain character ids while the callback table requires database UUIDs.
  Evidence: ordinary callback persistence calls `resolveDbCharacterId`, but the proposed close finalizer inserts checkpointed candidates directly and would roll back when a candidate such as `marcus` reaches the UUID foreign key unchanged.

- Observation: a generation lease does not fence a separate ordinary share insert.
  Evidence: after lease takeover, the expired holder can resume and win the unique beat-id share insert before its old token is rejected by `completeBeatGeneration`, making stale output canonical.

- Observation: the prompt audit misses worded fixed counts.
  Evidence: the active crosstalk builder says `one-sentence crosstalk reaction`; a search limited to digits and numeric ranges can pass while that generation instruction remains.

- Observation: the existing participant persistence drops required visitor narrative fields.
  Evidence: `MeetingParticipantSeed`, `createVisitorCharacter`, and `mapMeetingParticipantRow` omit `voiceExamples`, `lie`, `discomfortRegister`, `programRelationship`, `lostThing`, and `cleanTimeStart`. A successful participant save can therefore reload a visitor whose required three-line voice tuple and prompt-specific identity are absent.

- Observation: application rollback can strand active version-1 meetings on the legacy renderer.
  Evidence: the old page understands broad phases but not persisted `activeBeat` or `beatCursor`; removing the generic renderer while a version-1 meeting remains active can replay completed turns and append duplicate transcript entries.

- Observation: introduction is not a passable user gate in the shipped room flow.
  Evidence: the current page always submits the user's `introText()` as a real persisted share, including for listening-only meetings. Only later sharing-round gates expose a pass action.

- Observation: resolving a core character by the current `name` or `intro_style` columns cannot guarantee one database UUID.
  Evidence: `public.characters` has no unique core identity constraint, and `loadCharacterMaps` lazily inserts missing core rows during reads. Two concurrent cold requests can create duplicate Marcus rows before this plan's roster resolver runs.

- Observation: a checkpointed callback lifecycle target can become stale while a close lease is abandoned.
  Evidence: another meeting can reference or update the same callback after its target state was checkpointed. A recovered finalizer that checks only callback UUID would overwrite the newer reference with the older planned status.

- Observation: a zero-active-legacy query does not stop the old deployment from creating another legacy meeting before the renderer cutover finishes.
  Evidence: the current join insert and the proposed preflight are separate operations. A meeting created between them would carry no protocol version and open in the new page's read-only restart state immediately after a successful join.

- Observation: the callback scanner trusts relational ids emitted by the model.
  Evidence: its parser checks only field shapes. `originShareId` can name a share from another meeting, and `characterId` can name a character who did not speak the claimed origin line; the existing foreign keys do not enforce either meeting or speaker relationship.

- Observation: a stored user share without analysis can survive refresh while its user gate remains active.
  Evidence: user-share insertion, analysis persistence, and phase completion cannot be one transaction because analysis may call the model. After a crash at that boundary, the transcript shows the share but a renderer that sees only the active gate will ask the person to submit it again.

- Observation: a spoken share and a pass can both validate the same active user gate before either writes its effect.
  Evidence: a unique share beat id does not conflict with a separate pass-completion row. Without one serialized outcome boundary, the pass can advance the phase while the spoken request later inserts transcript content and schedules crisis work for the completed beat.

- Observation: persisting a room cue only when the browser acknowledges it exposes uncommitted transcript history.
  Evidence: the renderer shows the cue and waits before acknowledgment. A refresh during that pause can receive the still-active cue again because no canonical cue row proves that the visible moment already began.

- Observation: callback-version checks do not prevent database deadlocks when concurrent close transactions lock overlapping target rows in different orders.
  Evidence: two valid checkpoints can list the same callback UUIDs in reverse order. PostgreSQL may abort one transaction before it can return the plan's typed stale-version result unless every collection and table is locked in one canonical order.

- Observation: `ended_at is null` does not distinguish an active meeting from an abandoned browser tab.
  Evidence: a person can leave a legacy or version-1 room without calling `/close`. Waiting for every such row to end while `draining` rejects all inserts can otherwise keep new meeting creation unavailable indefinitely.

- Observation: controlled crisis resources are useful before generated support succeeds.
  Evidence: the planned flag was set only when accepted model prose completed. Provider failure or total quality rejection would leave the active crisis beat visible without the standard 988, 741741, and 911 card.

- Observation: a direct non-null activity column would break both existing rows and legacy application inserts during the additive phase.
  Evidence: the current `meetings` table has existing rows, and the old `createMeeting` path cannot supply a column it does not know. The migration must add the column nullable, backfill durable timestamps, add a database-owned default for old writers, and only then enforce non-null.

- Observation: meeting deletion can orphan sensitive generated checkpoints unless every new ledger follows the schema's existing cascade boundary.
  Evidence: `shares` and `meeting_participants` already reference `meetings(id) on delete cascade`; topic, user-gate, generation, and close-run rows otherwise retain meeting-derived text after the parent and ownership boundary disappear.

- Observation: sentence-count cleanup alone does not enforce the repository's empty-section rule.
  Evidence: current prompt builders can emit `Recent shares: None yet.` and `Callbacks referenced: - No callbacks referenced.`. Intake crisis may run before any transcript exists, so those placeholder sections need explicit removal and tests.

- Observation: `main`'s lockfile omitted the optional native `bufferutil@4.1.0` and `utf-8-validate@6.0.6` records required by its already-locked `ws@8.19.0` graph.
  Evidence: a clean `npm ci` initially returned `EUSAGE`; adding only those two registry-verified package records changed no existing lock entry. The focused core suite and type check then ran locally without Docker or a provider probe.

- Observation: the legacy opening's visible order conflicts with its persisted phase labels.
  Evidence: `app/src/routes/meeting/[id]/+page.svelte` emits the literal empty-chair line, Marcus's opening share, the `moment_of_silence` cue, then Chrystal's reading. `app/src/lib/core/ritual-orchestration.ts` instead transitions `OPENING -> EMPTY_CHAIR -> INTRODUCTIONS`, while `app/src/routes/meeting/[id]/share/+server.ts` associates `EMPTY_CHAIR` with Chrystal's reading. The plan requires the literal line to become a generated room moment and separately persists cues, but has not fixed their phase/cursor relationship.

## Decision Log

- Decision: Validate replay context before returning the same persisted beat object, and retain the existing persistence interaction union until the database-backed slice implements room entries.
  Rationale: Replay identity must not bypass roster integrity; the pure-core checkpoint must not promise unsupported database writes.
  Date/Author: 2026-09-15 / Codex, PR #96 review.

- Decision: define a beat as one stable, persisted instruction to the renderer rather than another phase.
  Rationale: phases remain useful broad states, but replay recovery needs to know the exact moment within a phase. A beat provides that resume point without replacing the established phase enum.
  Date/Author: 2026-09-12 / Codex

- Decision: keep beat selection pure in `app/src/lib/core/meeting-beats.ts` and keep all persistence in the database seam and server routes.
  Rationale: the repository requires core logic to be deterministic and free of fetch or database access. Pure selection can be exhaustively tested from saved state and roster fixtures.
  Date/Author: 2026-09-12 / Codex

- Decision: persist one `activeBeat` and an integer `beatCursor` inside `MeetingPhaseState`, and add a separate database phase version for compare-and-set writes.
  Rationale: returning an existing active beat makes retries stable. A version filter prevents two server instances from overwriting each other's state; the loser reloads the winner's state instead of inventing a second beat.
  Date/Author: 2026-09-12 / Codex

- Decision: introduce the beat contract in two pure checkpoints, with phase beat fields optional until a protocol-version-1 initializer writes them explicitly.
  Rationale: C01 can reject malformed normal beat payloads and C02 can add canonical crisis provenance without pretending a historical phase JSON is safe to execute. A later selection slice must gate new execution on the protocol version and explicit initialized fields rather than restarting an old meeting at cursor zero.
  Date/Author: 2026-09-14 / Codex

- Decision: apply the version-1 readiness gate before returning even an already active beat.
  Rationale: an unversioned or partial historical phase JSON cannot prove that its active payload belongs to the new protocol. Returning it merely because it looks structurally valid would let legacy state enter the new renderer without a trusted cursor or initialization record.
  Date/Author: 2026-09-14 / Codex

- Decision: derive every optional choice from the meeting id, phase, cursor, and roster, then persist the resulting beat before returning it.
  Rationale: production behavior needs variety across meetings and repeatability within one meeting. `Math.random()` and browser-local seeded choices do not provide a durable answer after refresh.
  Date/Author: 2026-09-12 / Codex

- Decision: give persisted generated entries a nullable stable `beatId` and enforce uniqueness on `(meeting_id, beat_id)` when a beat id exists.
  Rationale: sequence order says where an entry appears but not why it exists. The unique beat id lets a retry return the canonical stored entry instead of appending another generation.
  Date/Author: 2026-09-12 / Codex

- Decision: use one `/meeting/[id]/next` protocol to claim a beat, acknowledge non-generation beats, and persist topic choices; generated-share, user-share, crisis, and close routes complete their own active beats.
  Rationale: the client needs one way to ask what happens next, while existing specialized routes still own generation and their side effects. The protocol avoids a second page-level phase machine.
  Date/Author: 2026-09-12 / Codex

- Decision: persist a selected topic and completion of its active topic beat in the same version-checked meeting-row update.
  Rationale: two separate writes allow concurrent submissions to leave one request's topic paired with the other request's phase transition. One compare-and-set update makes the winning version authoritative for both values.
  Date/Author: 2026-09-13 / Codex

- Decision: regenerate the orchestration portion of `+page.svelte` as a small generic beat loop after the server path is proven.
  Rationale: debugging the nested `runRound*` chain would preserve the duplicate source of truth. Transcript rendering, SSE display, crisis presentation, and input components remain valuable; the hand-written sequence functions are the replaceable part.
  Date/Author: 2026-09-12 / Codex

- Decision: persist the generated empty-chair text as a non-user share with null `characterId` and interaction type `empty_chair`, and render that type as a ritual entry with no speaker label.
  Rationale: it is a room moment rather than a character performance. Reusing the shares table gives it ordering, refresh, and beat-id idempotence without adding a second transcript store.
  Date/Author: 2026-09-12 / Codex

- Decision: validate every generated empty-chair candidate before persistence with the same minimum authenticity and voice-consistency scores used for character shares.
  Rationale: a room moment is still generated text shown to a vulnerable user. A dedicated room-voice validation prompt can return the existing quality schema; a rejected candidate leaves the beat active and cannot enter the transcript.
  Date/Author: 2026-09-13 / Codex

- Decision: after a user-share insert wins or loses its unique beat id, derive crisis, heavy-disclosure, significance, and phase outcome from the canonical stored share.
  Rationale: this makes a race converge on one content value and one interpretation. The losing request must never advance the meeting using text that the transcript did not keep.
  Date/Author: 2026-09-13 / Codex

- Decision: make close a durably claimed, beat-keyed workflow with a stored canonical response.
  Rationale: the active beat alone does not prevent two requests from running summary, memory, callback, and lifecycle work before either advances phase state. A unique close-run claim lets only its holder run side effects, and a completed record gives all retries the same response.
  Date/Author: 2026-09-13 / Codex

- Decision: expansion accepts only the target share id and derives topic and recent transcript from owned server state.
  Rationale: prompt and quality context affect generated output and must follow the same server-authority rule as character shares and close.
  Date/Author: 2026-09-13 / Codex

- Decision: inject one server-clock timestamp into pure beat initialization and completion.
  Rationale: a function that calls `new Date()` is not deterministic even if its other inputs match. The clock seam supplies the value once; core transition helpers receive it as data and tests use a fixed timestamp.
  Date/Author: 2026-09-13 / Codex

- Decision: model crisis support as an explicit beat carrying the canonical triggering share id, and remove the unused reflection user gate.
  Rationale: crisis generation must reload the exact stored user text and owner identity. Reflection is the presentation reached after close; it requires no user submission, so `finished` can render it without an uncompletable gate.
  Date/Author: 2026-09-13 / Codex

- Decision: probe the migration against the real local Supabase stack and capture contention results before implementing its mock or adapter.
  Rationale: phase compare-and-set, first-analysis-wins, partial unique indexes, and leased close claims depend on actual Postgres/PostgREST behavior. The privacy plan establishes the pinned local tool and configuration, so this plan can preserve seam order without creating hosted infrastructure.
  Date/Author: 2026-09-13 / Codex

- Decision: promote specialized route migration and the generic renderer as one cutover, while keeping their implementation assignments separate.
  Rationale: the current page sends legacy bodies and no beat ids. Requiring active beats in those routes before the renderer changes would stop the shipped room. Small subagent slices remain useful, but `BEAT-L` through `BEAT-Q` cannot land on `main` independently.
  Date/Author: 2026-09-13 / Codex

- Decision: atomically persist every room cue before the server first exposes its active beat.
  Rationale: cue text is controlled rather than generated, but it becomes observable meeting history as soon as the renderer shows it. Claiming the beat and inserting its canonical cue row in one transaction lets refresh restore the same entry during the presentation pause; acknowledgment only advances the already durable beat.
  Date/Author: 2026-09-13 / Codex

- Decision: claim generated character and room-moment beats before any model stream reaches a client.
  Rationale: a unique row written after SSE output is too late to keep competing clients canonical. A leased generation claim allows one stream producer, makes competitors wait, and points completed retries to the stored share.
  Date/Author: 2026-09-13 / Codex

- Decision: namespace close callback effect keys with the meeting id and hydrate reflection from the stored meeting summary.
  Rationale: beat ids can repeat across meetings, so a globally unique callback key needs meeting scope. The summary must enter page data so refresh after a lost close response still renders the completed reflection.
  Date/Author: 2026-09-13 / Codex

- Decision: apply the existing quality thresholds and the generation claim to crisis support before persistence.
  Rationale: crisis text is a generated share with the same authenticity and voice requirements as every other visible share. A bounded validation loop prevents rejected or competing candidates from reaching the user, and total rejection leaves the support beat retryable.
  Date/Author: 2026-09-13 / Codex

- Decision: represent crisis provenance as a typed canonical trigger and record when an intake trigger has completed.
  Rationale: a user-share trigger points to its owned row, while an intake trigger points to the authorized meeting snapshot. Persisting the trigger and an `intakeCrisisHandled` flag makes refresh stable and prevents the same setup text from restarting crisis support forever.
  Date/Author: 2026-09-13 / Codex

- Decision: atomically advance the close beat and complete its run through one database RPC.
  Rationale: successful finalization must leave both the meeting and its canonical close response committed or neither. An expired claimant can safely replay earlier idempotent effects and retry this one transaction without interpreting a half-finished state.
  Date/Author: 2026-09-13 / Codex

- Decision: include the close-summary builder and every empty-section branch in the prompt-constraint repair, and use the preview composition's Grok mock for browser verification.
  Rationale: sentence-count and empty-section rules apply to every prompt in this flow. A missing transcript or callback list must omit its heading and body rather than emit `None yet` or `No callbacks referenced`; route-backed refresh tests need deterministic generation and quality results inside the server process without contacting a provider.
  Date/Author: 2026-09-13 / Codex

- Decision: persist a terminal `skipped` generation outcome only for a fully rejected character share.
  Rationale: the room must advance without showing sub-threshold text, and retries must know that this character was intentionally omitted. Crisis support and the empty chair remain active on total rejection because neither has a safe skip policy.
  Date/Author: 2026-09-14 / Codex

- Decision: share one deterministic fallback-roster function between the page loader and `/next`.
  Rationale: participant persistence is allowed to degrade, so every reader must derive the same seats from the meeting id and stored start time when no rows are available. The server beat cannot reject a roster the shipped page already accepted.
  Date/Author: 2026-09-14 / Codex

- Decision: make the beat id and cursor authoritative for completion while recording round participation as an idempotent set.
  Rationale: the same character may complete distinct crosstalk and scheduled-share beats. Repeating one beat is rejected by its id; repeating a person across different beats is valid and must not inflate the unique-participant list.
  Date/Author: 2026-09-14 / Codex

- Decision: retry an in-progress claim automatically for a bounded period and classify transcript speakers by user status and room interaction before character id.
  Rationale: refresh should converge on work another holder is finishing, while cancellation and a bounded stop keep the renderer controllable. Room-owned entries must remain visibly and semantically distinct from user speech in both UI and server prompts.
  Date/Author: 2026-09-14 / Codex

- Decision: resolve terminal beat-owned evidence before requiring the beat to remain active.
  Rationale: ownership and request validation still run first, but a stored share plus analysis, completed generation result, skipped result, crisis result, or completed close run is sufficient to answer a retry after phase advancement. Only unfinished work requires an active matching beat.
  Date/Author: 2026-09-14 / Codex

- Decision: put every durable close mutation in one token-checked finalization transaction and renew the claim during preparation.
  Rationale: model work and candidate discovery can be checkpointed without mutation. The meeting completion fields, finished phase, callback inserts and lifecycle states, and close-run result either commit together or not at all, while renewal prevents a healthy slow worker from being taken over.
  Date/Author: 2026-09-14 / Codex

- Decision: activate the beat renderer only for meetings created with `meeting_protocol_version = 1`.
  Rationale: translating a legacy mid-round cursor would guess about already-visible turns. During cutover, an immutable protocol switch routes an in-progress unversioned meeting only to the frozen legacy renderer and a version-1 meeting only to the beat renderer. The legacy path is removed after every such meeting completes or expires; it never calls `/next`.
  Date/Author: 2026-09-14 / Codex

- Decision: persist `crisis_resources_visible` in the same transaction that enters a canonical crisis beat and hydrate one controlled resource payload from that flag.
  Rationale: the emergency numbers are standard product copy and do not depend on model output. Intake crisis claim or spoken-share crisis completion sets the monotonic fact before support generation begins, so provider failure, quality rejection, lost responses, and refresh all retain the card without storing duplicate prose.
  Date/Author: 2026-09-14 / Codex

- Decision: centralize persisted-or-fallback roster resolution for every server consumer.
  Rationale: the pure derivation alone is insufficient if individual routes still load participant rows differently. One server resolver supplies identical character profiles to page load, `/next`, character generation, close, and expansion.
  Date/Author: 2026-09-14 / Codex

- Decision: keep topic completion in its narrow beat ledger and serialize every spoken-or-pass user-gate submission through a separate outcome row.
  Rationale: topic choice, its phase update, and its terminal outcome must commit together. A user gate has a different race: spoken content and a pass must contend for the same `(meeting_id, beat_id)` row so exactly one can own the beat. The spoken winner points to its atomically inserted share and remains active for analysis; the pass winner advances without transcript text.
  Date/Author: 2026-09-14 / Codex

- Decision: define `CharacterShareInteractionType` as `ShareInteractionType` without `room_cue` or `empty_chair`.
  Rationale: the broader persisted transcript union can include room-owned entries, while the character-beat union and runtime validator must reject those values before `/share` generation.
  Date/Author: 2026-09-14 / Codex

- Decision: ensure stable character identities independently before a fallback roster becomes selectable.
  Rationale: a nullable unique identity key scoped as `meeting:<meeting-id>:visitor:<domain-character-id>` makes visitor creation idempotent without colliding when two meetings start in the same second, even when meeting-participant persistence fails. The resolver returns derived seats only after every core id resolves and every visitor has a durable UUID target, so later share and callback writes cannot strand on identity lookup.
  Date/Author: 2026-09-14 / Codex

- Decision: atomically insert a generated share and complete its generation run under the current unexpired claim token.
  Rationale: the token must fence the first durable output write, not merely a later ledger update. A stale holder that resumes after takeover cannot insert a share or increment participant state; the current holder commits the canonical row and completed run together.
  Date/Author: 2026-09-14 / Codex

- Decision: lock close-finalization rows in one fixed table order and sort every row set by UUID before `FOR UPDATE`.
  Rationale: lifecycle versions detect logical staleness but do not control lock acquisition. Locking meeting, run, sorted origin shares, sorted characters, then sorted callbacks makes concurrent overlapping closes wait or return a typed version conflict instead of deadlocking from reversed checkpoint order.
  Date/Author: 2026-09-14 / Codex

- Decision: give both protocols a durable activity timestamp and a 24-hour inactivity expiry, while a temporary bridge routes each meeting to exactly one renderer.
  Rationale: new joins need not remain blocked while old rooms drain, and an abandoned row cannot live forever. Expiry locks and rechecks the row against a server-clock cutoff, preserves its transcript, marks it abandoned rather than completed, and makes every later route return the same read-only restart state.
  Date/Author: 2026-09-14 / Codex

- Decision: stage `last_activity_at` through nullable add, durable backfill, database default, then non-null enforcement.
  Rationale: the additive migration must accept both preexisting meetings and unstamped inserts from the still-deployed legacy application. A database-owned timestamp default keeps those inserts valid until all successful mutations explicitly advance activity.
  Date/Author: 2026-09-14 / Codex

- Decision: make every meeting-scoped ledger reference `meetings(id) on delete cascade` and prove parent deletion removes its rows.
  Rationale: a close checkpoint contains meeting-derived summary and callback text. It must remain inside the same ownership and deletion lifecycle as the meeting rather than survive as an orphan.
  Date/Author: 2026-09-14 / Codex

- Decision: resolve callback candidate domain ids to validated database UUIDs before checkpointing close output.
  Rationale: finalization must remain one transaction without bypassing the adapter's identity mapping. A read-only batch resolver supplies persistence-ready candidates; missing or ambiguous mappings stop before checkpoint/finalization, and the RPC validates the referenced rows again while locked.
  Date/Author: 2026-09-14 / Codex

- Decision: persist and rehydrate the complete immutable profile for every generated visitor.
  Rationale: a durable UUID alone is not a usable prompt subject. The persistence seam must retain the required real three-line `voiceExamples` tuple and every narrative field, return the stored snapshot on retry, and reject incomplete or placeholder profiles before a visitor can enter the roster.
  Date/Author: 2026-09-14 / Codex

- Decision: require zero active version-1 meetings before removing the beat renderer during rollback.
  Rationale: stopping new version stamps prevents growth but does not make existing beat cursors legible to the old page. A temporary protocol bridge keeps those rows on the beat renderer while new unversioned meetings use the legacy renderer; completed or safely expired rows leave the active count before that renderer is removed.
  Date/Author: 2026-09-14 / Codex

- Decision: allow a stored pass outcome only for `user_gate:share`.
  Rationale: listening-only skips later round sharing while preserving the user's introduction. Topic uses its own outcome and an introduction always stores the canonical intro share.
  Date/Author: 2026-09-14 / Codex

- Decision: require the durable core character identity slice in epic #79 to merge before `BEAT-F` or any roster-resolving database work begins.
  Rationale: this plan may add meeting-scoped visitor identities, but it cannot safely resolve core ids while reads can lazily create duplicate rows. The prerequisite migration must seed core characters once, give each an immutable unique slug, reconcile or reject duplicates, and remove read-time creation before this plan treats core UUID lookup as canonical.
  Date/Author: 2026-09-14 / Codex

- Decision: fence every checkpointed callback lifecycle target with the callback's expected lifecycle version and recompute only that part of a close checkpoint when it becomes stale.
  Rationale: the close token protects one meeting's worker, not a callback shared across meetings. Versioned callback writes keep a recovered close from retiring or downgrading a callback that another meeting referenced after checkpointing, while preserving the already generated summary, memory, and candidate outputs.
  Date/Author: 2026-09-14 / Codex

- Decision: enforce protocol cutover through a database singleton and insert trigger with `legacy`, `draining`, and `version_1` modes.
  Rationale: a database fence covers every old and new application instance during a rolling deployment. In `draining`, all meeting inserts fail recoverably while the protocol-bridge application deploys. After every serving instance can route old and new rows by immutable protocol, changing to `version_1` reopens stamped creation while existing legacy rows finish or expire on the frozen legacy path; a late old instance still cannot create an unreadable meeting.
  Date/Author: 2026-09-14 / Codex

- Decision: validate every callback candidate against the exact canonical transcript supplied to scanning before identity resolution or checkpoint.
  Rationale: model instructions do not enforce foreign-key relationships. The origin share must be an eligible share in this meeting, the candidate character must be that share's speaker, and the quoted text must occur in its content; finalization locks and checks the same origin share and resolved character UUID.
  Date/Author: 2026-09-14 / Codex

- Decision: resume a partially persisted user share from its beat id without another user submission.
  Rationale: the canonical share already contains every input needed for crisis, heavy-disclosure, significance, and phase completion. The user-share endpoint can reload it when content is omitted, while hydration detects that the active gate already has a transcript row and automatically invokes this recovery path instead of showing the input again.
  Date/Author: 2026-09-14 / Codex

- Decision: replace exact sentence ranges in every ritual prompt used by the beat engine with plain guidance such as `brief`, `concise`, and `naturally complete`.
  Rationale: the new engine must not spread a known prompt-rule violation. This is a narrow constraint repair, not a general rewrite of character voice or the style constitution.
  Date/Author: 2026-09-12 / Codex

## Outcomes & Retrospective

Planning is complete and the C01-C03 pure-core checkpoint is implemented in PR #96; route and database integration remain unimplemented. The plan intentionally replaces only the meeting-script portion of the large Svelte page. It preserves the working transcript UI and specialized server seams, creates one durable server answer for each next moment, and makes transcript persistence the authority after a retry or refresh. Its reviewed recovery contracts cover the dangerous boundaries that point-in-time checks missed: creation stays database-fenced throughout deployment, a temporary protocol bridge prevents draining or abandoned meetings from blocking new rooms, the activity column remains compatible with old creators, meeting deletion cascades through every new ledger, callback rows lock in canonical order and must match their transcript origins, controlled crisis resources persist before generation, empty prompt sections disappear rather than announce placeholders, spoken and pass submissions contend for one gate outcome, a spoken share persisted before analysis resumes from its beat id, and a room cue is durable before the renderer may show it. Database-backed work waits for epic #79 to establish one unique, migration-seeded UUID for every core character. The planned beat migration now follows the private and identity migrations with its own Supabase version.

The seven milestones are ordered by real dependency. Epic #79's durable core identity is a precondition for database-backed Milestone 2, while the pure Milestone 1 can proceed independently. Versioned persistence makes the contract safe under retry. The endpoint proves the server can answer before consumers migrate. Route completion then makes side effects idempotent. The generated empty-chair path lands before the renderer depends on it. Only after those gates pass does the client delete its old sequence, and the final milestone proves replay-free refresh across the completed flow.

Isolated offline work completed the first three small pure-engine checkpoints. The code now has a tested pure vocabulary for normal beats, typed crisis triggers, controlled resources, compatible phase fields, and deterministic opening selection. It returns a ready active beat unchanged and fails closed when protocol-version-1 readiness is absent; it does not complete a beat, touch a route, create persistence state, change prompt text, or make the production meeting flow live. C04 must first record the opening ritual's generated-moment, cue, and Chrystal-reading phase/cursor order; its current title alone is not a safe license to choose one from the inconsistent legacy implementations.

## Context and Orientation

The meeting's broad state lives in `MeetingPhaseState` in `app/src/lib/core/types.ts`. `app/src/lib/core/ritual-orchestration.ts` initializes that state, validates phase transitions, and records which characters and whether the user spoke. It does not currently describe the next moment inside a phase.

The meeting page is `app/src/routes/meeting/[id]/+page.svelte`. It renders a transcript, streams generated character text, presents one input control at a time, and handles crisis and closing. It also contains the entire meeting script. The functions `runFreshMeeting`, `runRoundOne`, `runRoundTwo`, `runRoundThree`, `runClosing`, and `continueFromPersistedPhase` call one another, choose speakers, insert local ritual text, and wait between moments. These are the functions this plan replaces.

Character generation runs through `app/src/routes/meeting/[id]/share/+server.ts`; user speech runs through `user-share/+server.ts`; crisis support and closing have their own child routes. The database contract is `app/src/lib/seams/database/contract.ts`, with a mock in `app/src/lib/seams/database/mock.ts` and Supabase adapter in `app/src/lib/server/seams/database/adapter.ts`. `meetings.phase_state` stores the serialized phase JSON. `shares` stores transcript content in `sequence_order`.

A beat is one renderer instruction with a stable id. A character-share beat names a roster character and interaction type. A room-cue beat contains a controlled cue key such as `moment_of_silence`, which the UI maps to existing presentation text. A generated-room-moment beat asks the server to generate the empty-chair entry. A user-gate beat says which input the room awaits: introduction, topic, or share. A crisis-support beat carries a typed canonical trigger and the server-selected responder. A close beat invokes the existing close workflow. A finished beat leaves the post-meeting reflection visible and stops polling; there is no separate reflection gate because the current product asks for no reflection input.

Claiming a beat means storing it as the phase state's `activeBeat` before returning it. Completing a beat means validating its id against the active beat, applying its recorded outcome, clearing it, incrementing the cursor, and persisting the next phase state. Compare-and-set means the database update includes the version that was read; if another request changed that row first, the update changes zero rows and the route reloads instead of overwriting the newer state.

A user-share analysis is the stored interpretation of one canonical user entry: whether it indicates crisis, whether it is a heavy disclosure, and its significance score. The first valid analysis written for a beat wins. Requests that raced with different text or model output reload that stored interpretation before they can advance the phase. If the share row persisted but analysis did not, page hydration hides the already completed input and posts only the beat id so the route can finish from canonical stored content.

A crisis trigger is either `{ source: 'user_share', shareId }` or `{ source: 'meeting_intake' }`. The first form reloads the exact owned user transcript row. The second reloads `userMind` from the owner-checked meeting record established by the privacy plan. Neither form carries browser-supplied prose. `intakeCrisisHandled` records that setup-triggered support finished so the same stored intake does not open another crisis sequence after refresh.

The crisis resource payload is controlled application copy, exported once from pure core code. `crisis_resources_visible` is the durable meeting-level fact that the room entered canonical crisis support. The transaction that claims an intake-triggered crisis beat or completes a spoken user gate into a share-triggered crisis sets it before any support model call. The page and route response map true to the standard sticky payload even when generation fails, every candidate is rejected, the response is lost, or no crisis beat remains active.

A generation run is a short leased claim keyed by meeting and beat for character, crisis-support, or generated-room text. The holder is the only request allowed to call the model. Competing requests return a retryable in-progress response or the completed stored share. The holder renews its lease while generation and validation continue so a slow valid generation is not mistaken for a crashed worker. Candidate output remains server-buffered until it passes quality checks. One token-fenced transaction verifies the current unexpired lease, resolves the character persistence identity, inserts the canonical share, applies participant share state, and marks the generation run completed; only that stored text may then be presented progressively.

A skipped generation is a terminal character-only run outcome with no share id and a controlled `quality_rejected` reason. It records that every bounded candidate failed the quality gate. The character route completes that beat without adding transcript text; a retry returns the same skip and never regenerates it. Crisis support and the generated room moment cannot use this outcome.

A room cue is controlled text selected by a cue key in the pure beat engine rather than prose returned by a model. The database transaction that first claims the cue beat also persists its key, rendered text, beat id, and non-user status in the shares transcript before `/next` may return it. Acknowledgment only advances that already durable beat. This gives a refresh during the presentation pause the same stable record used for generated and user entries.

A user-gate outcome is one database row keyed by meeting and beat. It records either `spoken` with the canonical user-share id or `pass` with no share. Both submission paths lock the meeting and contend for this same key before inserting transcript text or advancing phase. A spoken winner leaves the gate active while its canonical content is analysed; a pass winner advances atomically. The losing path returns the stored outcome and cannot apply its own content, analysis, crisis transition, or phase update.

A fallback roster is the existing core-plus-visitor selection derived through one pure helper from the meeting id and the meeting record's stable `startedAt`. One server-side resolver loads persisted participants and otherwise calls that helper when rows are empty or a non-not-found read/save fails. Before it returns derived seats, a separate idempotent persistence seam resolves every core character and inserts or loads each visitor through a meeting-scoped unique identity key together with its complete immutable profile; a participant-association failure cannot erase those UUID targets or narrative fields. The page loader, `/next`, `/share`, `/close`, and `/expand` all use the resolver, so a derived visitor keeps the same durable identity, required voice tuple, profile, and name throughout the meeting. An idempotent round-participation set records who has taken part, while the beat cursor records how many distinct scheduled moments completed.

Closing needs a stronger claim because it performs several effects before the active beat can clear. A close run is one durable row keyed by meeting and beat. Its claim token is an unguessable value held by the worker allowed to proceed. Its lease is a bounded time during which competing requests wait instead of repeating work, and the holder renews it during every slow pre-checkpoint stage. Before checkpoint, a pure validator binds each model candidate to an eligible share in the exact scanned meeting, that share's speaker, and quoted text found in its content; a read-only persistence resolver then converts the surviving character ids to validated database UUIDs. A checkpoint contains validated generated output plus those persistence-ready candidates and the complete intended database mutation set, without applying it. Each lifecycle target includes the callback version used to derive that state. If the worker crashes and the lease expires, a new worker resumes the checkpointed generated work, but a callback changed by another meeting invalidates the lifecycle targets. The finalization RPC locks the meeting and run first, then sorted origin-share UUIDs, sorted character UUIDs, and sorted callback UUIDs through ordered `FOR UPDATE` queries. It revalidates relationships and versions, aborts all writes on a bad relationship or stale lifecycle version, and otherwise commits meeting completion fields, the exact `finished` phase, callbacks, lifecycle target states, and the canonical run response in one transaction. Reversed checkpoint arrays therefore cannot reverse lock order and deadlock a valid close.

`meeting_protocol_version` distinguishes meetings created for this beat contract from historical phase JSON. Version 1 meetings use `/next` and the generic renderer. During cutover, an in-progress unversioned meeting uses only a frozen legacy renderer; a completed historical meeting renders its stored transcript and summary without sequence work. `last_activity_at` records the last successful meeting mutation, and `abandoned_at` marks a meeting whose server-verified inactivity exceeds 24 hours. An abandoned meeting preserves its transcript, renders a read-only restart presentation, and performs no model or database mutation. The cutover singleton serializes meeting inserts against mode changes: `legacy` permits only unstamped creation, `draining` permits none during deployment, and `version_1` permits only trusted stamped creation. Once the bridge deployment is verified, `version_1` may reopen new rooms while older unversioned rows finish or expire on their own renderer.

The privacy plan adds a hook-level owner check to every `/meeting/[id]` request. All endpoints in this plan are created below that route and must rely on the common gate. They must never weaken it or add a second inconsistent owner rule.

## Requirements

`BEAT-01` requires one pure function to return the existing active beat or deterministically choose the next beat from persisted state, meeting context, the centrally resolved persisted-or-deterministic roster, and transcript facts. `BEAT-02` requires the chosen beat to be persisted before exposure and protected by a versioned compare-and-set write; claiming a room cue must persist its canonical transcript row in that same transaction before `/next` returns it. `BEAT-03` requires one canonical stored effect per beat id, including quality-accepted generated and crisis entries committed under their current lease token, a terminal rejected-character skip, pre-exposure room-cue rows, topic completions, mutually exclusive spoken-or-pass user-gate outcomes, recoverable user-share interpretation, and the fully atomic close result. Every completion route must return terminal evidence before requiring an active match. Character beats accept only character interaction values; room-owned cue and empty-chair values are invalid. Epic #79 must first make every core character's immutable slug unique, seed those rows by migration, and remove lazy read-time creation. Every selected fallback character then has one durable persistence identity and a complete validated narrative profile. Callback candidates must match the canonical scanned meeting share, its speaker, and its quoted content; checkpoints contain resolved database UUIDs plus expected lifecycle versions, stale targets are recomputed, and every finalizer locks tables and sorted UUID sets in the same order. `BEAT-04` requires the client to render server beats without hardcoded round or speaker sequencing, automatically resume a stored-but-unanalysed user share, and retry a valid in-progress claim for a bounded period. `BEAT-05` requires refresh at every user gate, including the partial user-share boundary and pass-versus-content contention, during every room-cue pause, after every generated, skipped, or crisis-support entry, and after close to restore transcript, sticky crisis resources, and reflection without growth, loss, duplicate solicitation, or false speaker attribution. `BEAT-06` requires intake- and user-share-triggered crisis interruption and recovery to preserve a canonical source, persist controlled resources when the crisis beat begins regardless of later model success, and keep the truthful resume point. `BEAT-07` requires a fresh persisted empty-chair moment that follows all prompt and quality constraints. `BEAT-08` requires the room's topic choice and completion state to survive refresh. Only protocol-version-1 meetings may enter the new sequence; a temporary immutable protocol switch keeps active unversioned rows on the frozen legacy renderer. A database-backed creation fence must remain closed through deployment of that bridge, then stamped creation may reopen while old meetings complete or leave the active count through a server-clocked 24-hour inactivity transition. Rollback uses the symmetric bridge so active version-1 state never reaches the legacy renderer.

## Implementation Slices

These are the maximum assignment units for Codex or a subagent. Assign one slice at a time, with exclusive ownership of its files during that turn. A passing slice returns paths, test evidence, and any newly discovered dependency to the main agent; it does not continue into the next label without a new assignment.

- `BEAT-A` adds the beat union, including a character-only interaction subtype, typed intake and user-share crisis triggers, backward-compatible phase-state fields, the controlled crisis resource payload, and validator tests in `core/types.ts` and the narrow contract tests that exercise those types.
- `BEAT-B` implements only the opening, empty-chair, cue, and introduction selection in `core/meeting-beats.ts` with table-driven pure tests.
- `BEAT-C` adds topic, three-round, closing, and finished selection to the same pure engine after `BEAT-B` passes. It does not touch routes.
- `BEAT-D` injects the server-supplied transition time and adds beat-id completion, idempotent round-participation recording for repeated speakers, deterministic optional branches, listening-only share-gate skips after the required introduction, both canonical crisis-trigger sources, the intake-trigger handled marker, resume behavior, and the direct close-to-finished path to the pure engine. It stops at core tests.
- `BEAT-E` repairs numeric and worded sentence-count instructions in every generation prompt builder in `prompt-templates.ts`, including the active crosstalk path, plus the private `buildCloseSummaryPrompt` in `meeting.ts`, after reading `style-constitution.ts`. It also removes empty recent-share, callback, and other optional sections entirely instead of emitting `None yet`, `No callbacks referenced`, or equivalent placeholders. It owns those files and their existing specs; it changes no workflow behavior.
- `BEAT-F` begins only after epic #79's core-identity migration, duplicate reconciliation or explicit duplicate failure, unique immutable slug, migration seed, and deletion of lazy read-time creation have merged. It then adds the database contract and uniquely versioned migration `20260912000300_server_owned_meeting_beats.sql`, including protocol version; a nullable-add/backfill/database-default/non-null sequence for last activity; abandonment state; a `legacy | draining | version_1` meeting-creation fence and enforcing insert trigger; a server-clocked inactivity-expiry RPC; a unique visitor identity key and complete visitor profile snapshot; durable crisis-resource visibility and room-cue fields; a callback lifecycle version; topic-completion, user-gate, generation-run, and close-run ledgers whose meeting foreign keys all cascade on delete; canonical user-share analysis; token-fenced atomic generated-share completion; and one transaction for every durable close mutation. It updates validators but does not author fixtures, mock behavior, contract tests, or adapter queries.
- `BEAT-G` adds `probes/serverOwnedMeetingBeatsProbe.mjs`, runs it against the real local Supabase stack established by the privacy plan, and writes redacted creation-fence transitions and rejected-insert, legacy-insert activity default, protocol bridge and inactivity expiry, parent-meeting cascade deletion across every new ledger, core-identity preflight, visitor-identity and complete-profile, introduction-pass rejection, atomic cue claim, topic completion, spoken-versus-pass gate contention, pre-generation crisis visibility, fenced generated-share, completed/skipped generation, analysis and partial-user-share recovery, callback transcript-relation rejection, callback-id resolution, callback lifecycle-version conflict and recomputation, deterministic reversed-overlap lock contention, close-renewal, and full atomic-finalization captures. It stops if a real probe cannot run and never hand-authors substitute fixtures.
- `BEAT-H` implements the fixture-backed mock's creation fence, protocol and activity fields, legacy-insert activity default, idempotent inactivity expiry, parent-meeting cascade deletion, idempotent character identity and complete visitor-profile hydration, versioned phase, atomic cue claim, topic-completion and user-gate-outcome lookup, crisis-resource-on-entry update, token-fenced generation commit and character skips, stale-holder rejection, first-analysis-wins and beat-id-only partial-share recovery, atomic topic completion, mutually exclusive spoken/pass submission, callback transcript validation, persistence-id resolution and lifecycle versions, renewable close claim, stale lifecycle-checkpoint refresh, deterministic finalizer lock order, and full atomic-finalization behavior, then adds contract tests for schema conformance and mock fidelity.
- `BEAT-I` implements the captured behavior in the Supabase adapter with focused creation-fence transition and insert rejection, protocol/activity/abandonment, legacy-insert default, idempotent expiry, cascade deletion, visitor-identity and complete-profile reload, introduction-pass rejection, version-win, version-conflict, crisis-resource-on-entry, topic-completion replay, atomic cue claim, spoken-versus-pass contention, token-fenced completed/skipped generation, stale-holder takeover, stored-beat, user-analysis race and partial recovery, callback transcript relation and id resolution, callback lifecycle compare-and-set and stale-checkpoint refresh, ordered-lock contention, close-claim renewal, in-progress, full atomic-finalization, and completed-result tests.
- `BEAT-J` extracts one pure deterministic fallback-roster helper and one server-side persisted-or-fallback resolver used first by the page loader and `/next`, ensures every derived character identity and complete canonical profile before those seats become selectable, then creates the empty-request claim behavior and route spec. It returns an existing active beat unchanged; when selecting a new room cue, it atomically persists both the active phase state and canonical cue row before returning either; when selecting intake crisis support, it atomically sets crisis-resource visibility before returning the beat.
- `BEAT-K` adds room-cue and topic acknowledgments to `/next`. Cue acknowledgment only completes the pre-persisted active cue; topic acknowledgment uses the atomic beat-keyed topic completion, lost-response replay to the canonical current beat, and version-conflict recovery.
- `BEAT-L` migrates `share/+server.ts` to resolve the canonical roster, return a stored completion before checking active state, derive unfinished generation from an active character beat, acquire its generation claim before streaming, commit accepted text and the run atomically under that token, reject a resumed stale holder, and persist/return a terminal skip when every bounded candidate is rejected.
- `BEAT-M` migrates `user-share/+server.ts` and then `crisis/+server.ts` to terminal-result-first active-beat completion. Spoken input and a later share-gate pass contend through one user-gate outcome key; the spoken winner and its share insert commit together, while the pass winner and phase advancement commit together. The slice requires a persisted introduction, rejects introduction passes, accepts beat-id-only recovery only from the canonical spoken outcome, and includes canonical-share analysis, typed share-or-intake triggers, crisis-resource visibility committed with entry into support before generation, a generation claim, and token-fenced atomic support persistence after bounded quality validation. These two routes are paired because the user-share result can create the crisis-support beat consumed by the second route; no other route belongs in this slice.
- `BEAT-N` migrates `close/+server.ts` and hardens the pure candidate validation in `core/callback-scanner.ts`. It adds terminal-result-first handling, the renewable close claim, centrally resolved roster, truthful user/room/character transcript labels, server-loaded transcript context, relational validation against the exact scanned shares before read-only callback persistence-id resolution, versioned pure lifecycle planning, stale-checkpoint recomputation, and one finalization transaction with fixed table order and sorted UUID locks. It stops when concurrent, reversed-overlap lock ordering, slow-holder renewal, completed-retry, expired-lease recovery, cross-meeting origin rejection, mismatched-speaker rejection, callback-id, callback-version conflict, room-entry labeling, and every mutation rollback case passes.
- `BEAT-O` makes only `expand/+server.ts` use the central roster resolver and derive its topic, truthfully labeled room/user/character transcript, and recent shares on the server. It stops when forged client context is rejected and both generated prompts receive canonical context.
- `BEAT-P` adds the `/room-moment` completion endpoint, returns stored completion before checking active state, acquires a generation claim, quality-validates, atomically commits the accepted share and run under that token, rejects a resumed stale holder, and tests the generated empty-chair beat before any renderer depends on it. It may touch the already-established route branch points but must not reopen round sequencing.
- `BEAT-Q` is the protocol cutover: it makes trusted meeting creation stamp version 1, maps a fenced insert to a recoverable join-unavailable response, makes the page loader expose stored summary and crisis-resource visibility, and routes by server-loaded protocol. Version 1 uses the new generic renderer loop with bounded `Retry-After` claim retry; null protocol continues through the frozen existing sequence until completion or expiry and never calls `/next`. Specialized routes preserve their existing request contract only for authorized unversioned rows and use strict beat completion only for version 1. During hydration, the beat renderer detects an active user gate whose beat id already has a spoken outcome and invokes beat-id-only recovery without showing an input. This slice owns the narrow `createMeeting` adapter/composition change, loader bootstrap mapping, protocol branches in the migrated routes, `+page.svelte`, and direct tests. The browser cannot choose the protocol.
- `BEAT-R` adds a fixture-backed scripted scenario to the existing Grok mock, installs it through the preview-process composition, builds the refresh, terminal-retry, legacy-bridge, inactivity-expiry, fenced-cutover, partial-user-share, spoken-versus-pass, cue-pause, pre-generation-crisis-resource, listening-only introduction, sticky-crisis-resource, skipped-character, persisted and fallback visitor-profile, repeated-speaker, in-progress-claim, callback-relation, reversed-lock-order, and room-label matrix in the route integration suite and `app/tests/e2e`, and removes the resolved replay item from `DEFERRED.md` only if every case passes.
- `BEAT-S` runs the full nonfixture verification, rehearses the short database-fenced bridge deployment and version-1 activation, exercises server-clocked expiry until zero active legacy rows, removes the frozen legacy orchestration and unversioned route branches only after that gate, proves the symmetric rollback bridge and zero-active-version-1 cleanup, performs the final stale-code search, and updates the plan and governance files with actual evidence.

The deliberately narrow slices reflect the risk of this migration. `BEAT-Q` writes the new renderer as a separate authority selected only by immutable protocol while freezing the old sequence for existing rows. `BEAT-S` is the regenerate-over-debug deletion gate: it removes the entire legacy orchestration and compatibility branches after completion or safe expiry proves no active row depends on them. `BEAT-L` through `BEAT-Q` are separate assignment and review units on one integration branch, but together form one decision-gated promotion. Do not merge any of those six slices to `main` alone. The earlier slices are additive contract, probe, fixture-backed mock, adapter, or endpoint outcomes.

## What Not To Do

Do not patch replay guards into `continueFromPersistedPhase` or add more flags to the `runRound*` chain. Freeze those functions for unversioned meetings during the bridge and remove them in `BEAT-S` after every dependent row completes or expires.

Do not let the browser select a speaker, phase, interaction type, sequence order, optional branch, or persisted topic. Do not keep a production switch that allows both the old client script and the server beat engine to advance one meeting.

Do not promote active-beat-only versions of `share`, `user-share`, `crisis`, or `close` while an unversioned page still sends legacy requests. During the bounded bridge, select the request contract from the server-loaded protocol: unversioned meetings retain frozen legacy behavior and version 1 requires the beat contract. Remove the legacy branches at the zero-active cleanup gate; do not preserve them as a long-lived compatibility path.

Do not revive a missing legacy beat cursor as zero and run the new renderer. Do not infer a cursor from speaker counts or unkeyed shares. Stamp only newly created beat-protocol meetings as version 1. Until cleanup, route a non-abandoned unversioned row only to its frozen legacy renderer; after safe expiry, preserve its transcript and show the explicit read-only restart path.

Do not treat an active-count query as a creation fence. Put the database in `draining` before bridge deployment and leave it there until every serving instance can route both protocols and stamp version 1. Then activate `version_1` and reopen stamped creation while legacy rows finish or expire; the database trigger must still reject unstamped inserts in `version_1` and every insert in `draining`.

Do not let an abandoned row keep a renderer or global creation fence alive forever. Update `last_activity_at` only with successful server-owned meeting mutations. Expiry uses the server clock, locks the candidate, rechecks 24 hours of inactivity, sets `abandoned_at` without inventing completion fields, and leaves the transcript read-only. A concurrent real mutation and expiry cannot both win.

Do not add `last_activity_at not null` in one step or require the legacy application to supply it during additive deployment. Add it nullable, backfill from durable meeting/share timestamps, install a database-owned default for old inserts, then enforce non-null before expiry can run.

Do not create a meeting-scoped topic, user-gate, generation, or close ledger without `meeting_id references public.meetings(id) on delete cascade`. Parent deletion must remove checkpoints and every other derived row inside the same transaction.

Do not use `Math.random()`, current time, request order, or unstable database row order to choose a beat or id. Do not pass an unversioned meeting through the beat compare-and-set contract. Do not store a topic separately from completing its active topic beat. Do not analyze a losing user-share payload after another payload has become canonical. Do not treat a second generated string as equivalent to returning the first persisted result.

Do not show a user gate when its beat id already belongs to a stored user share. A refresh at that partial boundary must call the beat-id-only recovery path, reuse or finish analysis from the canonical row, and advance from its stored outcome without asking for the text again.

Do not strand `/next` on an empty participant table after the page loader used its deterministic fallback. Do not derive fallback seats through separate code paths or an unstable current timestamp. Do not treat `charactersSpokenThisRound` as a prohibition on distinct beats from the same character; the beat id prevents duplicate completion, while the participation list remains an idempotent set.

Do not run summary generation, memory extraction, callback scanning, lifecycle planning, or phase completion before acquiring the unique close claim. Do not let that claim expire silently during pre-checkpoint model work; renew it with the current token and stop before mutation if renewal loses ownership. Do not return an invented close response while another claim is active, and do not rerun close work after a completed result exists. Do not write meeting completion, phase, callbacks, lifecycle, or run completion in separate transactions.

Do not emit character, crisis-support, or room-moment content before acquiring that beat's generation claim, validating the complete candidate, and persisting the canonical winner. Do not persist a crisis response below authenticity 6 or voice consistency 6; if no candidate passes, leave the support beat active. Do not return a newly claimed room cue until the same transaction has persisted its controlled cue key, text, beat id, and active phase state. Do not construct a globally unique callback effect key from a beat id that omits meeting scope.

Do not insert a generated share through ordinary `appendShare` and check the lease token afterward. The current unexpired token must fence the share insert, participant update, and completed generation-run result in one transaction. An expired worker that resumes after takeover must write nothing.

Do not leave a fully rejected character beat active for unbounded regeneration, and do not advance it without a terminal persisted skip. Do not use that skip outcome for crisis support or the empty chair. Do not turn a valid in-progress lease into a terminal page error; honor `Retry-After` with bounded, cancellable requests for the same beat.

Do not require an active beat before looking up an already stored share, analysis, skip, generation result, crisis response, or close result. Terminal evidence answers a lost-response retry; only a request that still needs work must match the active beat.

Do not treat a stored topic alone as proof that any supplied beat id completed it. Persist a meeting-scoped topic-completion row in the same transaction as the topic and phase change, then use that exact evidence for retry. Do not let spoken input and pass use independent tables or transactions: both must contend for one user-gate outcome row before either transcript insertion or phase advancement. Do not create fake transcript rows for topic or pass outcomes with no spoken text.

Do not append a room cue locally and wait until acknowledgment to make it durable. The `/next` response must carry the canonical stored cue row; refresh during the pause upserts that row by share id, resumes only the remaining delay from its stored creation time, and acknowledges once without replaying the reveal animation.

Do not require a share id for an intake-triggered crisis or copy `userMind` into a synthetic share. Load the typed trigger from phase state, then load its text from the matching canonical server record. Do not let completed intake support retrigger on every refresh.

Do not accept topic, recent transcript, speaker, or other prompt context from the browser for expansion. The request identifies the owned share; the server loads every other input from persisted meeting state.

Do not infer a transcript speaker from `characterId === null`. Test `isUserShare` first, then classify `room_cue` and `empty_chair` as room-owned entries, and only then resolve an ordinary character. Use the same pure mapping in page hydration, close context, and expansion context.

Do not wait for generated support to persist crisis resources. Set the visibility flag atomically when the canonical crisis beat begins, return the controlled card before any provider call, and keep it visible when generation fails, validation rejects every candidate, support later completes, or the response is lost.

Do not load persisted participants directly in a roster-consuming route after the central resolver exists. A fallback visitor selected by `/next` must resolve to the same profile in `/share` and the same name in close and expansion prompts.

Do not return a derived visitor from the resolver until its stable identity key resolves to a durable character UUID. Keep identity upsert independent from the meeting-participant association so an association failure cannot make later share persistence impossible.

Do not begin `BEAT-F` while core character reads can create rows or while a core slug can resolve to more than one UUID. Do not copy epic #79's migration into this plan. Require its unique immutable core slug, migration seed, duplicate disposition, and removal of lazy creation to be merged and proven first.

Do not persist only the visitor's base display fields. Store and runtime-validate the complete immutable generated profile, including exactly three real `voiceExamples` and every narrative field, and rehydrate that stored snapshot when participant rows load. Do not synthesize missing examples or accept placeholder lines.

Do not let `character_share.interactionType` widen when room-owned transcript values are added. Its type and runtime validator must reject `room_cue` and `empty_chair`; those values complete only through their own beat kinds and endpoints.

Do not checkpoint callback candidates containing domain ids such as `marcus` where the finalizer expects UUID foreign keys. Resolve them in a read-only batch, validate the UUID mapping, and have the finalizer revalidate the locked character rows. Missing or ambiguous identity stops close before checkpoint.

Do not trust a model-produced callback `originShareId`, speaker, or quote because it passed shape validation. Require the origin id to be an eligible canonical share supplied to the scanner for this meeting, require the candidate character to equal that share's non-null speaker, and require normalized `originalText` to occur in the share content. Finalization must lock and recheck the same meeting/share/character relation before insert.

Do not finalize a checkpointed callback lifecycle target by UUID alone. Every ordinary reference or lifecycle update increments its lifecycle version. The close checkpoint records the version it planned from, and finalization must reject the complete transaction if any locked target has changed. Recompute those lifecycle targets from current rows under the current close token before retrying; never overwrite a newer callback state with an abandoned checkpoint.

Do not lock callback, character, or origin-share rows in checkpoint array order. Deduplicate and sort UUIDs, then acquire locks in the fixed meeting, close-run, origin-share, character, callback table order. Two overlapping close transactions with reversed target arrays must wait and converge through version handling rather than deadlock.

Do not limit the prompt audit to digits. Remove fixed numeric ranges, digit counts, and worded counts such as `one-sentence` from every generation prompt in `prompt-templates.ts` and the close-summary builder, and keep a source search that detects both forms.

Do not render headings for optional prompt sections when their collections are empty. Omit the heading and body together; strings such as `None yet`, `No callbacks referenced`, and `SECTION: none` are prohibited substitutes.

Do not accept a pass for the introduction gate. The renderer must persist `introText()` as the user's canonical introduction in both ordinary and listening-only meetings; only a later `user_gate:share` may produce a pass control outcome.

Do not remove the version-1 renderer or specialized completion routes while any non-abandoned version-1 meeting remains active. Transition the database from `version_1` to `draining`, deploy a bridge that keeps those rows on the version-1 renderer while it can create and render legacy meetings, then switch to `legacy` and reopen unversioned creation. Existing version-1 rows finish or expire on their original renderer. Remove that renderer only after `ended_at is null and abandoned_at is null` reaches zero for version 1.

Do not follow the archived plan's fixed branch, old checkout paths, frontend-owned ordering, or ban on an orchestration endpoint. Do not split the entire large Svelte page before deleting its obsolete orchestration; issue #90 follows this work.

Do not rewrite character voices, the style constitution, therapy blocklist, or general generation system. Do not add exact sentence counts, forced physical actions, archetype labels, placeholder voice examples, or empty `SECTION: none` blocks. Do not show a candidate below the existing authenticity and voice-consistency thresholds.

Do not add a stock empty-chair fallback, show beat ids or phase machinery in the UI, call live providers in deterministic tests, construct the real Grok adapter in the mock preview composition, provision production infrastructure, or reimplement the core identity and cleanup epics inside this plan.

## Plan of Work

### Milestone 1: define the pure beat engine and compliant ritual prompts

Create `app/src/lib/core/meeting-beats.ts` and `app/src/lib/core/meeting-beats.spec.ts`. Add the beat types to `app/src/lib/core/types.ts`. Use a discriminated union so each kind carries only valid fields. The public contract must express at least these kinds: `character_share`, `room_cue`, `generated_room_moment`, `user_gate`, `crisis_support`, `close_meeting`, and `finished`. Every variant includes `id`, `ordinal`, `phase`, and `pauseAfterMs`. Define `CharacterShareInteractionType = Exclude<ShareInteractionType, 'room_cue' | 'empty_chair'>`; character beats require `characterId` plus that narrowed interaction, and their runtime validator rejects both room-owned values. Room cues require a controlled `cue`; user gates accept only `introduction`, `topic`, or `share`; a generated room moment requires `moment: 'empty_chair'`; and a crisis-support beat requires a `CrisisTrigger` discriminated as `{ source: 'user_share'; shareId: string }` or `{ source: 'meeting_intake' }` plus the server-selected `responderCharacterId`. Do not emit a reflection gate: post-meeting reflection is the UI for `finished`.

Extend `MeetingPhaseState` with `beatCursor: number`, `activeBeat: MeetingBeat | null`, `crisisTrigger: CrisisTrigger | null`, and `intakeCrisisHandled: boolean`. Its validator may still revive historical JSON for read-only display, including conversion of an earlier `crisisTriggerShareId` into the typed `user_share` form, but must never manufacture an actionable cursor for an unversioned meeting. Only a meeting whose stored `meeting_protocol_version` is 1 may pass phase state into `nextMeetingBeat`; its initialization writes all four fields explicitly. A missing protocol version or missing beat field is a sequence gate, not an instruction to restart at cursor zero.

Export a pure `nextMeetingBeat(input)` function. If `input.phaseState.activeBeat` exists, return that exact value without consuming randomness or changing state. Otherwise choose the next beat from the current phase, cursor, persisted participants, stored topic and mind context, listening choice, and compact transcript facts. A compact fact is a count, last interaction type, or stable id needed for sequencing; never pass the entire database adapter into core. Build beat ids from stable inputs, for example `<phase>:<cursor>:<kind>:<stable-subject>`. Do not use timestamps, array insertion order that is not part of the persisted roster, or `Math.random()`.

Export a pure `completeMeetingBeat(state, beatId, outcome, now)` function. The `now` value is a required `Date` supplied by the server clock seam. Refactor the ritual initialization and transition helpers used by this path to accept that same value instead of calling `new Date()` internally. The function rejects a mismatched id and uses that persisted id plus `beatCursor` as the completion authority. For character beats, add the character to `charactersSpokenThisRound` only if absent; a second distinct beat from that character is valid and increments the cursor without duplicating the participant. Do not call the existing duplicate-rejecting `recordCharacterSpoke` from this path or use the unique participant count to decide whether the scheduled beat sequence is complete. Apply a legal phase transition where appropriate, clear `activeBeat`, and increment `beatCursor`. A topic outcome must be validated against an exported `TOPIC_OPTIONS` moved from `+page.svelte`; persistence of that valid topic occurs at the server seam in Milestone 4. A crisis outcome stores the canonical typed trigger while recording `preCrisisPhase`; crisis completion returns to that saved point and clears the trigger without replaying completed work. Completing an intake-triggered support sequence also sets `intakeCrisisHandled` before the trigger clears. Completing close computes the `finished` state that the atomic finalization RPC persists with the canonical response.

Write table-driven tests for the full opening through finished sequence, every user gate, the required introduction followed by listening-only automatic passes only at later share gates, the three rounds, optional crosstalk and hard-question branches, a crosstalk beat followed by a scheduled share from the same character, crisis interruption and recovery tied to one canonical user share, clean-join crisis tied to stored intake, no intake retrigger after support, empty direct-engine roster rejection, stable roster ordering, retry of an active beat, invalid completion, and runtime rejection of `room_cue` or `empty_chair` as a character interaction. The repeated-speaker case must increment the cursor for both beat ids while the unique participation list contains the character once. Test two meeting ids that select different optional branches and repeat each input with one fixed timestamp to prove it is stable. Then vary only the supplied timestamp and prove only the intended phase-time field changes. A source search must find no `new Date()` or `Date.now()` in the new beat engine or the transition helpers it calls.

Before wiring the engine to generation, repair every fixed sentence-count instruction in `app/src/lib/core/prompt-templates.ts`, including numeric ranges, digit counts, and worded forms. This covers the character introduction, active `buildCrosstalkReactionPrompt`, meeting opening, topic acknowledgment, ritual opening, ritual introduction, ritual reading, topic introduction, ritual closing, empty-chair, expansion, memory, and other generation builders in that file. Also replace `Target one paragraph (4-6 spoken-style sentences).` in the private `buildCloseSummaryPrompt` in `app/src/lib/core/meeting.ts` with natural brevity guidance. In the same owned builders, remove each optional section entirely when its input is empty. This specifically removes the current `Recent shares:` plus `None yet.` block and `Callbacks referenced:` plus `- No callbacks referenced.` block, including the no-callback quality-validation case; apply the same rule to any other empty list or value rather than substituting new sentinel prose. Preserve each prompt's intent, update `prompt-templates.spec.ts` and `meeting.spec.ts` to test meaning, forbidden count language, present nonempty sections, and absent headings and placeholder bodies for empty inputs, and read `style-constitution.ts` before making these edits. Do not change workflow behavior, character foundations, require physical actions, pass archetype labels to generation, weaken the three-example voice contract, or fill empty prompt sections with placeholders.

This milestone is complete when the pure suite shows the same input always returns the same next beat, the canonical experience reaches `finished`, a repository search finds no numeric or worded fixed sentence-count instruction in any generation builder in `prompt-templates.ts` or the private close-summary builder, and focused prompt tests prove empty recent-share, callback, and other optional inputs omit both their headings and placeholder bodies. It satisfies `BEAT-01` and prepares `BEAT-06` through `BEAT-08`.

### Milestone 2: make beat state and generated entries retry-safe

Do not begin this milestone until epic #79 is merged. Verify its migration has one immutable, unique core-character slug, inserts or reconciles exactly one row for every core profile, and that `loadCharacterMaps` no longer inserts missing core characters during reads. A duplicate slug, missing seed, or remaining lazy insert blocks this dependency chain; do not compensate inside the beat migration with a name lookup that can return either row.

Create `app/supabase/migrations/20260912000300_server_owned_meeting_beats.sql`, following `20260912000100_private_meeting_intake.sql` and the merged `20260912000200_durable_core_character_identity.sql` from #79. Supabase uses each leading fourteen-digit numeric segment as the version; the descriptive suffix is not an ordering counter. Add `phase_version bigint not null default 0`, nullable `meeting_protocol_version smallint` constrained to 1 when present, `crisis_resources_visible boolean not null default false`, activity, and abandonment fields to `public.meetings`. Stage activity in this exact order: add `last_activity_at timestamptz` nullable; backfill every existing row from the greatest durable server timestamp among `started_at`, `ended_at` when present, and the latest owned `shares.created_at`; set a database-owned `default statement_timestamp()` so the deployed legacy `createMeeting` path can keep inserting unstamped rows; assert no null remains; then set `not null`. Add nullable `abandoned_at timestamptz`. Existing rows remain unversioned. A meeting is active only when both `ended_at` and `abandoned_at` are null; abandonment preserves the transcript and never fabricates completion fields. Probe a post-migration insert made in `legacy` without an explicit activity field and require a non-null database timestamp before any application writer changes.

Create a one-row `public.meeting_protocol_cutover` table with a fixed singleton key, `mode` constrained to `legacy`, `draining`, or `version_1`, a compare-and-set version, and an update timestamp. Seed it idempotently as `legacy`. A `BEFORE INSERT` trigger on `public.meetings` reads that singleton `FOR SHARE` and holds the lock through the insert transaction; the transition RPC updates the same row, so creation and mode changes serialize. The trigger enforces the locked mode: `legacy` accepts only a null `meeting_protocol_version`; `draining` rejects every insert with a stable database error code mapped by the server to a recoverable join-unavailable response; and `version_1` accepts only `meeting_protocol_version = 1`. Add a service-role-only compare-and-set RPC that permits only adjacent transitions `legacy -> draining -> version_1` and `version_1 -> draining -> legacy`; direct `legacy <-> version_1` transitions fail. This database trigger, rather than an application preflight alone, covers old instances during deployment. `createMeeting` begins supplying protocol version 1 only in the final cutover that installs the new renderer; the value is selected by trusted server composition and is never accepted from the browser.

Add nullable `source_identity_key text` to `public.characters` with a named unique constraint for visitors; PostgreSQL permits multiple historical nulls while the constraint gives Supabase upsert a direct conflict target. This visitor key is separate from the unique core slug established by #79. Add nullable `source_profile jsonb` for the immutable complete generated visitor snapshot. It contains every base field required by `CharacterProfile` and every `CharacterNarrativeProfile` field in JSON-safe form, including three nonempty real `voiceExamples`; `cleanTimeStart` is stored as an ISO timestamp and revived as a `Date` at the contract boundary. A check constraint requires identity key and source profile to be either both null for historical/core rows or both nonnull on a visitor row. New derived visitors use `meeting:<meeting-id>:visitor:<domain-character-id>` and an idempotent insert-or-load operation, preventing collisions between meetings started in the same second; historical rows remain null. The conflict path returns the stored canonical snapshot rather than overwriting it with a newly derived profile. Add nullable `beat_id text`, nullable `user_analysis jsonb`, and nullable `room_cue text` to `public.shares`, plus a partial unique index on `(meeting_id, beat_id)` where `beat_id is not null` and a unique constraint on `(meeting_id, beat_id, id)` for composite gate-outcome references. `user_analysis` is valid only for user shares and contains canonical `crisis`, `heavy`, and `significanceScore` values. `room_cue` is valid only for a non-user row with interaction type `room_cue` and stores a controlled `RoomCue` key; the row's content stores the exact rendered cue text. Replace the `shares_interaction_type_check` constraint with the existing values plus `room_cue` and `empty_chair`. Add `lifecycle_version bigint not null default 0` to `public.callbacks`; every reference count, last-reference time, status, parent, or other lifecycle mutation must compare the expected version and increment it in the same write.

Create `public.meeting_generation_runs` with `meeting_id uuid not null references public.meetings(id) on delete cascade`, `beat_id`, a generation kind limited to `character_share`, `crisis_support`, or `empty_chair`, `status`, `claim_token`, `lease_expires_at`, nullable `share_id`, nullable controlled `skip_reason`, timestamps, and primary key `(meeting_id, beat_id)`. Its statuses are `running`, `completed`, `skipped`, and `failed`. A `completed` row requires a share id and no skip reason. A `skipped` row requires kind `character_share`, no share id, and `skip_reason = 'quality_rejected'`; the other kinds can never be skipped. Atomic RPCs claim a run, renew the current token's lease, commit an accepted generated share under that token, terminally skip a rejected character, or fail it. The commit RPC locks the run, requires its matching token, `running` status, and unexpired lease, resolves and validates the character UUID when present, inserts the beat-keyed share, applies the participant share-count update, stores its id on the run, and marks the run completed in one transaction. A stale token writes none of those effects. A completed claim resolves and returns that share; a skipped claim returns the same terminal reason; an unexpired running claim returns `in_progress`; a failed or expired claim can be acquired again.

Create `public.meeting_topic_completions` with `meeting_id uuid not null references public.meetings(id) on delete cascade`, `beat_id`, one validated `topic`, `phase_version_after`, `created_at`, and primary key `(meeting_id, beat_id)`. One RPC locks the meeting, validates the active matching topic beat and expected phase version, inserts its canonical completion, and writes the topic, completed phase state, and next version together. A conflicting retry returns the stored topic. If any insert or update fails, neither the topic row, meeting topic, nor phase changes.

Create `public.meeting_user_gate_outcomes` with `meeting_id uuid not null references public.meetings(id) on delete cascade`, `beat_id`, `gate`, `outcome_kind`, nullable `share_id`, nullable `phase_version_after`, and timestamps, with primary key `(meeting_id, beat_id)`. Constrain `outcome_kind` to `spoken` or `pass`; `spoken` requires a share id, while `pass` requires `gate = 'share'`, a null share id, and a completed phase version. A composite foreign key `(meeting_id, beat_id, share_id)` references `shares(meeting_id, beat_id, id)`, and the spoken RPC additionally validates that row is marked as the user and contains no room-cue key. Store no prose or analysis in this ledger. Three service-only RPCs serialize on the meeting row and this key. The spoken-claim RPC validates an active introduction or share gate and atomically inserts both the beat-owned user share and its `spoken` outcome while leaving the gate active for analysis. The spoken-completion RPC requires that same outcome and share, requires its canonical analysis, then writes the ordinary or crisis phase state, next version, and `phase_version_after` together. The pass RPC validates an active share gate and atomically inserts the `pass` outcome with the completed phase state and next version. If either outcome already exists, submission RPCs return it: a pass winner prevents later share insertion, and a spoken winner prevents later pass advancement. Introduction can never enter the pass path.

The same migration creates `public.meeting_close_runs` with `meeting_id uuid not null references public.meetings(id) on delete cascade`, `beat_id`, `status`, a random `claim_token`, `lease_expires_at`, nullable validated `checkpoint`, nullable validated `result`, timestamps, and primary key `(meeting_id, beat_id)`. The checkpoint holds canonical generated outputs, callback candidates with deterministic effect keys, lifecycle target states paired with the exact callback `expectedLifecycleVersion` used to derive each target, one `completedAt` value, meeting summary and notable moments, the validated final phase state, and the canonical endpoint result before product mutations begin. Add nullable `close_effect_key` to `callbacks` with a partial unique index for non-null values so a checkpointed candidate is inserted at most once. The controlled close statuses are `running`, `completed`, and `failed`. A first claim inserts `running`; an unexpired running row reports `in_progress`; a completed row returns its stored result; and a failed or expired row may be claimed with a new token. Only the current claim token may renew, checkpoint, refresh the lifecycle portion of a checkpoint, atomically finalize, or fail the run. Deleting the parent meeting must cascade through this checkpoint and every other new meeting-scoped ledger in the same transaction.

Implement generation and close transitions as narrowly scoped Postgres functions exposed through Supabase RPC so claim insert/conflict inspection, lease renewal or takeover, token validation, and status updates are atomic. The close finalization function acquires locks in one invariant order: meeting row, close-run row, deduplicated origin-share UUIDs sorted ascending, deduplicated character UUIDs sorted ascending, then deduplicated lifecycle-target callback UUIDs sorted ascending. Each multirow query uses an explicit `ORDER BY id FOR UPDATE`. Before applying anything, it verifies the current token, unexpired lease, expected meeting phase version, named `close_meeting` beat, checkpoint/result agreement, each origin's meeting id, speaker UUID, and normalized quoted-text containment, and every callback's expected lifecycle version. Define normalization identically at both validation layers: translate ASCII `A-Z` to `a-z`, trim ASCII space, tab, carriage return, line feed, and form feed from both ends, collapse each internal run of those characters to one ordinary space, and preserve every other code point including punctuation. The SQL helper used by finalization must match the pure helper's captured examples. An origin or callback mismatch returns a typed conflict and rolls back the complete transaction; lifecycle version drift specifically returns `STALE_CALLBACK_CHECKPOINT`. On a valid checkpoint it writes `meetings.ended_at`, summary, notable moments, the checkpointed `finished` phase and next version, callback rows, version-incremented lifecycle target states, the validated canonical result, and completed run status in one transaction. Successful meeting mutations also advance `last_activity_at` from the server clock. If any validation, insert, or update cannot apply, none of those durable close mutations apply. The functions qualify table names explicitly, validate all controlled values, and are executable by the service role rather than public callers. Every migration statement and function replacement must be safe to retry; constraint replacement should use the same explicit drop-and-add pattern already used by the March roster migration.

In `app/src/lib/seams/database/contract.ts`, add `beatId: string | null`, `userAnalysis: UserShareAnalysis | null`, and `roomCue: RoomCue | null` to `ShareRecord`; add `meetingProtocolVersion: 1 | null`, `crisisResourcesVisible: boolean`, `lastActivityAt: Date`, nullable `abandonedAt`, nullable `summary`, and nullable `notableMoments` to `MeetingRecord`; and add `room_cue` and `empty_chair` to `ShareInteractionType` in `app/src/lib/core/types.ts`. Define `PersistableMeetingParticipant` as the existing participant seed plus required `CharacterNarrativeProfile`, preserving `voiceExamples` as a nonoptional three-tuple; define `CharacterPersistenceRecord { domainCharacterId, databaseCharacterId, profile }` where `profile` is complete and runtime-validated; and define the `UserShareAnalysis` shape, a validated `MeetingCloseResult` matching the close endpoint's complete response, a generation-claim result with `acquired`, `in_progress`, `completed`, and `skipped` variants, and a close-claim result with `acquired`, `in_progress`, and `completed` variants. Adjust the `createMeeting` input type to omit completion-only meeting fields and the server-owned protocol choice. Extend runtime validators only at this point. Historical entries and incomplete meetings use null. New beat-owned entries use the stable id. Do not create fixtures or edit the mock, contract tests, or adapter before the probe below succeeds.

Add `MeetingProtocolCutoverMode = 'legacy' | 'draining' | 'version_1'`, a read result carrying mode and version, and service-only read and compare-and-set transition operations to the database seam. Meeting creation maps the trigger's draining or protocol-mismatch code to a stable recoverable error instead of reporting an arbitrary 500. The landing action keeps the entered form values and tells the user the room is briefly unavailable; retrying after activation creates a new version-1 meeting once and never creates an unstamped row.

Define `MEETING_ABANDONMENT_MS` as 24 hours and add a service-only bounded expiry operation such as `expireInactiveMeetings({ protocolVersion, cutoff, now, limit })`. It selects candidates in meeting-id order with `FOR UPDATE SKIP LOCKED`, then rechecks `ended_at is null`, `abandoned_at is null`, and `last_activity_at <= cutoff` under the row lock before setting only `abandoned_at`. The operation takes its cutoff and timestamp from the server clock, preserves transcript and summary fields, and never records a fake close. Every successful server-owned meeting mutation locks the same meeting row and advances `last_activity_at` in its transaction, so expiry and new activity cannot both win. A route that finds `abandoned_at` returns the stable read-only transcript plus clean-join result and performs no model or database mutation.

Add a versioned phase read that returns `{ phaseState: MeetingPhaseState | null, version: number }` and a compare-and-set update accepting `meetingId`, `expectedVersion`, `phaseState`, and an optional validated meeting patch. Its only allowed product patch is the monotonic `crisisResourcesVisible: true` when the new state enters crisis support; the generic operation can never clear the resource flag, write topic or close fields, or complete a user gate. Topic completion uses the topic RPC, and spoken or pass completion uses the user-gate RPCs. The Supabase update must filter by both meeting id and `phase_version = expectedVersion`, write the new phase state, version `expectedVersion + 1`, and server-owned `last_activity_at`, and include the resource flag in that same row update when requested. Report `applied: false` when zero rows changed. Keep existing phase methods only while routes are migrated; remove or delegate them at the end so no route can silently bypass versioning.

Add `claimNextMeetingBeat({ meetingId, expectedVersion, phaseState, roomCueEffect? })`, or an equivalent service-only RPC. It locks the meeting, rejects a stale version or abandoned meeting, and persists the newly selected active phase state and `last_activity_at` before returning it. When the selected beat is intake-triggered crisis support, it also sets `crisis_resources_visible = true` in that transaction before any model call. For a `room_cue`, `roomCueEffect` is required and must match the beat's controlled cue key, canonical rendered text, beat id, null character id, and non-user interaction; the RPC inserts that transcript row and the active phase state atomically. Other beat kinds reject `roomCueEffect`. Reading an existing active beat remains side-effect free and returns its existing cue row when applicable. No newly selected cue or crisis beat may be exposed if its phase, cue, resource, or activity write fails.

Add `getShareByBeatId({ meetingId, beatId })`. It returns `NOT_FOUND` for no entry. Make beat-aware generated-share handling converge on the stored row when the unique index reports that another request already wrote the same beat. User rows are created only by the spoken user-gate RPC, and controlled cue rows only by the atomic beat-claim RPC; neither may use an ordinary append path.

Add `ensureCharacterPersistenceIdentities({ meetingId, participants })`, accepting complete persistable participants. It resolves every core domain id to an existing UUID and static complete profile. For each visitor, it idempotently inserts or loads the meeting-scoped `source_identity_key` plus immutable `source_profile`, returning `CharacterPersistenceRecord[]` with the canonical stored profile. The conflict path never overwrites the first profile. Rework `saveMeetingParticipants` to use this operation rather than an unfenced visitor insert, and make `getMeetingParticipants` rehydrate visitors from the same complete snapshot instead of constructing empty narrative fields from the base columns. Runtime validation requires every field and exactly three nonempty, non-placeholder voice examples before either path returns a protocol-version-1 roster. Also expose a read-only batch resolver taking the meeting id and callback candidate domain ids; core ids resolve through their canonical rows and visitors through the same meeting-scoped identity key. It returns validated UUIDs and fails on a missing or ambiguous row. Neither operation mutates meeting phase. The server roster resolver requires the first operation to succeed and uses the returned canonical profiles before returning fallback seats even when participant association later fails.

Add `getTopicCompletion({ meetingId, beatId })` and `completeTopic({ meetingId, beatId, expectedVersion, phaseState, topic })`, or equivalent names with the atomic semantics above. The route looks up that result before requiring an active beat. A completed retry validates the stored canonical topic rather than trusting the repeated body, then returns the already active current beat or claims the next beat if none is active.

Add `getUserGateOutcome({ meetingId, beatId })`, `claimSpokenUserGate({ meetingId, beatId, expectedVersion, gate, content })`, `completeSpokenUserGate({ meetingId, beatId, shareId, expectedVersion, phaseState })`, and `completeUserGatePass({ meetingId, beatId, expectedVersion, phaseState })`, or equivalent names with the serialized semantics above. A spoken result returns the canonical stored share even if a competing request supplied different text. A pass result contains no share and prevents later analysis or crisis work. `completeSpokenUserGate` verifies the same `spoken` outcome, share id, and stored analysis and atomically sets its completed version with the meeting phase, `last_activity_at`, and `crisis_resources_visible = true` when that phase enters crisis support. No route may advance a spoken gate from a bare share lookup or advance any user gate through the generic phase writer.

Add `claimBeatGeneration`, `renewBeatGeneration`, `commitBeatGeneration`, `skipCharacterGeneration`, and `failBeatGeneration`, or equivalent names with the migration's semantics. A character, crisis-support, or room-moment route checks for a stored share or terminal run first, then acquires the claim before invoking the model. Only the current token can renew, atomically commit, skip, or fail. `commitBeatGeneration` takes the accepted persistence input and performs the token/lease check, character-id resolution, beat-keyed share insert, participant-state update, share-id attachment, and completed status together. A completed retry loads that share. Skip accepts only a character run and stores no share. A skipped retry returns that outcome even after the active beat advanced, and the route completes a still-active matching character beat without generation or transcript insertion. Use a bounded lease comfortably longer than an ordinary provider call and renew it during generation and validation. No candidate content leaves the server before quality acceptance and the atomic commit, and a competitor or expired holder receives no generated chunks and writes no share.

Add `setUserShareAnalysisIfAbsent({ meetingId, beatId, analysis })`. It updates only a user share whose `user_analysis` is null, writes the same `significanceScore` into the existing score column, then returns the stored analysis whether this request won or lost. This is the interpretation claim: competing analyzers may propose different results, but every completion must use the one analysis the row retained. Contract and adapter validation reject analysis for a character share or invalid scores. The seam must also distinguish a stored beat-owned user share with null analysis from no share so a content-free recovery request can resume interpretation from the canonical row.

Add `claimMeetingClose({ meetingId, beatId, leaseDurationMs })`, `renewMeetingClose({ meetingId, beatId, claimToken, leaseDurationMs })`, `checkpointMeetingClose({ meetingId, beatId, claimToken, checkpoint })`, `refreshMeetingCloseLifecycleCheckpoint({ meetingId, beatId, claimToken, lifecycleTargets })`, `finalizeMeetingClose({ meetingId, beatId, claimToken, expectedVersion })`, and `failMeetingClose({ meetingId, beatId, claimToken, errorCode })`, or equivalent names with those semantics. Claim acquisition is the only boundary that authorizes close preparation. Renew the token periodically during summary, memory, fallback-summary, and callback-candidate model work and immediately before identity resolution, checkpoint, refresh, and finalization; losing renewal discards uncheckpointed output and stops before a product mutation. Split callback work into read-only candidate discovery, pure origin/speaker/quote validation against the scanned shares, batch resolution from domain ids to database UUIDs, and final persistence. Split lifecycle work into input loading plus a pure target-state plan whose entries include `callbackId`, `expectedLifecycleVersion`, and the intended state. Checkpointing rejects domain ids and preserves only persistence-ready callback candidates, deterministic `<meetingId>:<beatId>:callback:<checkpointed-index>` effect keys, versioned lifecycle targets, `completedAt`, meeting fields, final phase state, and endpoint response. The finalizer deduplicates UUIDs and locks the meeting, close run, sorted origin shares, sorted characters, and sorted callbacks in that fixed order before revalidating each origin, character, quoted-text relation, and lifecycle version. If it returns `STALE_CALLBACK_CHECKPOINT`, the current claim holder reloads every target, recomputes the pure lifecycle plan, and uses the token-checked refresh operation to replace only the lifecycle section before retrying finalization; the generated summary, memory, candidates, completion time, final phase, and response remain unchanged. Bound this loop and fail recoverably if callback contention does not settle. The finalizer is the only operation allowed to write meeting completion fields, insert callbacks, apply lifecycle targets, clear the close beat, or complete the run. A lost finalization response reloads the now-completed run and cannot observe a subset of those writes. An ordinary retry of a completed beat reads that response and does not call any generator, discovery, identity resolution, planning, or mutation seam. An unexpired competing claim produces a retryable conflict with `Retry-After`; it does not start a second close. Bound the lease, test a healthy slow holder that renews past the original expiry, and separately test takeover after a truly abandoned lease. Race close finalizers from two meetings whose checkpoints list overlapping callback targets in opposite orders; one waits, then either succeeds or returns the typed stale-version conflict without a deadlock.

Create `app/probes/serverOwnedMeetingBeatsProbe.mjs` and `probe:supabase-meeting-beats` before writing fixtures or implementations. Run the migration on the real local Supabase stack established by `plans/private-meeting-access-execplan.md`, after proving #79 left exactly one row per core slug and a read cannot add another. The probe first proves the creation fence: `legacy` accepts only unstamped meetings, `draining` accepts none, `version_1` accepts only stamped meetings, illegal direct mode transitions fail, and a stale compare-and-set cannot change the singleton. Before any application writer changes, it creates a legacy meeting without specifying `last_activity_at` and proves the database default supplies a non-null server timestamp. It proves the temporary bridge routes an active unversioned row only through frozen legacy behavior and a version-1 row only through beat behavior, while a server-clocked 24-hour expiry marks an inactive row abandoned without inventing completion and a concurrent mutation prevents expiry. It performs idempotent meeting-scoped visitor identity and full-profile persistence, unique core and callback-id resolution, concurrent version updates, atomic topic completion with replay, introduction-pass rejection, crisis-resource visibility in the transaction that enters each intake- or share-triggered crisis beat before generation, atomic room-cue claim with its pre-exposure transcript row, competing generation claims and lease renewal, token-fenced generated-share commit, a terminal character skip and replay, rejection of skip for crisis and empty-chair kinds, competing first-analysis writes, and recovery of a stored user share whose analysis is null. It races spoken content against pass twice, once with each path acquiring the meeting lock first, and proves exactly one gate outcome, at most one user share, no pass transcript row, and crisis support only when the canonical spoken content warrants it. It runs two simultaneous close claims, wrong-token renewal/checkpoint/finalization, a healthy slow close holder renewing past the original expiry, completed replay, and truly expired-lease takeover. It creates two meeting ids with the same start second and visitor slot, assigns different generated profiles, and proves their identity keys, UUIDs, and stored profiles remain distinct while retries within either meeting return its original UUID and complete profile. It persists participant associations, reloads the visitor through `getMeetingParticipants`, and proves all narrative fields and the exact three real voice examples survive. It lets a generation holder expire, gives the claim to a replacement, resumes the stale holder first, and proves the stale token inserts no share or participant update before the replacement atomically commits. It proves a rejected topic completion changes neither the ledger, topic, nor phase. It passes one valid callback candidate and rejects candidates naming another meeting's origin share, a different speaker, or text absent from the origin before checkpoint. It rejects a close checkpoint containing a domain callback id, then checkpoints a callback at lifecycle version N, abandons the lease, references that callback from another meeting so it becomes N+1, and proves recovered finalization returns `STALE_CALLBACK_CHECKPOINT` with no product mutation. Under the recovered token it reloads the callback, recomputes and replaces only the lifecycle targets, and finalizes against N+1. It separately rejects one finalization after preparing resolved callback UUIDs plus meeting, lifecycle, phase, and run changes and proves none applied. It races two finalizers whose overlapping callback targets arrive in opposite order and proves the fixed table and sorted UUID lock order avoids deadlock; the waiter then succeeds or receives the typed lifecycle conflict. It then performs a successful finalization, simulates a lost response by rereading, and proves every checkpointed mutation plus the identical completed result is present together. Finally, it populates topic, user-gate, generation, and close-run rows for one disposable meeting, deletes that parent, and proves all four ledgers have zero orphan rows. It records both forward and rollback bridge transitions. It resets its deterministic rows on repeat and writes redacted raw success, conflict, and failure shapes plus capture metadata to the database fixture directory. If that real probe cannot run, mark this milestone blocked and stop before the fixture, mock, contract-test, adapter, and route dependency chain; do not invent captures.

After inspecting a successful capture, extend the fixture-backed database mock and add contract tests for fixture schema conformance and mock fidelity. Cover creation-fence reads, compare-and-set transitions, and rejected meeting inserts; the database activity default for a legacy insert; immutable protocol selection through the temporary bridge; server-owned activity updates, bounded inactivity expiry, mutation-versus-expiry serialization, and abandoned read-only behavior; parent-meeting cascade deletion of every new ledger; idempotent meeting-scoped visitor identity; complete visitor-profile storage and reload with the required three-tuple; rejection of incomplete or placeholder profiles; callback-id mapping; callback origin-meeting, speaker, and quote validation; lifecycle-version increments on ordinary callback writes; version wins and conflicts; atomic topic completion and replay; atomic spoken-user-share claim; spoken-versus-pass serialization in both lock orders; introduction-pass rejection; atomic crisis-resource visibility before support generation; atomic cue-beat plus transcript claim and stable existing-cue lookup; nullable historical beat ids, cue keys, and user analysis; all three generation kinds; claim renewal; fenced share/run commit; stale-holder rejection after takeover; completed and skipped replay; invalid non-character skips; first-analysis-wins; beat-id-only recovery of a stored user share whose analysis is null; `empty_chair` with null character id; meeting-scoped callback keys; close-claim contention; domain-id checkpoint rejection; stale lifecycle-checkpoint rollback and token-checked refresh; deterministic fixed-table and sorted-UUID close locks under reversed overlapping targets; completed-result replay; wrong-token rejection; slow-holder renewal; expired-lease takeover; rollback of the full close mutation set; and lost-response reread. Only after the mock contract passes, implement the same captured behavior in `app/src/lib/server/seams/database/adapter.ts`, validate each RPC result, and add focused adapter tests. Update test doubles last.

This milestone is complete when #79 has already proven one migration-seeded row per core slug with no read-time creation, and the real beat probe, captured fixtures, mock-fidelity contract, and adapter suites pass in that order; the creation fence rejects all inserts in `draining`, rejects a late unstamped insert after `version_1` activation, and permits only adjacent compare-and-set mode changes; the staged activity migration leaves no null and a legacy insert that omits the field receives the database default; deleting a meeting removes all four new ledger families; the bridge keeps each existing meeting on its immutable renderer while safe inactivity expiry removes abandoned rows from active counts; a concurrent mutation defeats expiry and every successful mutation advances server-owned activity; a repeated visitor identity request returns one UUID and the same complete stored profile even if participant association fails; a successful association reload preserves the exact three voice examples and all narrative fields; two meetings started in the same second receive distinct visitor identities and profiles; a pair of writers using the same phase version produces one applied update and one conflict; topic completion stores one terminal result with its phase change; a newly claimed cue stores its active phase and transcript row together; crisis entry stores resource visibility before provider work; spoken and pass submissions produce one user-gate outcome in either race ordering while introduction pass is rejected; a stale generation holder writes no share after takeover and the replacement commits share plus run atomically; a rejected character run replays one terminal skip while other generation kinds reject that state; two user-share analyzers and a content-free recovery converge on one stored interpretation; callback candidates with a cross-meeting origin, wrong speaker, or absent quote are rejected before checkpoint; valid callback domain ids become validated UUIDs before checkpoint; two close callers produce one claim holder; a renewing holder cannot be taken over; reversed overlapping callback target arrays never deadlock because finalizers lock one table order and sorted UUID sets; a callback changed after checkpoint makes finalization apply nothing and forces lifecycle recomputation from its new version; a failed finalization or invalid locked origin relationship changes no meeting, callback, lifecycle, phase, or run row; and every successful or lost-response retry observes the complete atomic close result. It satisfies `BEAT-02` and `BEAT-03` at the persistence boundary.

### Milestone 3: add the next-beat endpoint

Create `app/src/routes/meeting/[id]/next/+server.ts` and route tests in `app/src/lib/server/routes/meeting-next-beat.spec.ts`. The endpoint is a `POST` because claiming or acknowledging a beat changes persisted state. Its body may be empty when asking for the next beat. For a `room_cue` acknowledgment it accepts `{ completedBeatId }`. For the topic gate it accepts `{ completedBeatId, topic }`. Reject unrelated fields, invalid topic values, mismatched beat ids, and attempts to acknowledge character, generated-room, user-share, crisis-support, close, or finished beats through this generic path. The specialized routes named below complete every rejected effectful kind; the engine emits no reflection gate.

Before any beat phase, participant, or transcript work, require the owner-checked meeting record to have `meetingProtocolVersion === 1` and `abandonedAt === null`. The bridge routes an active unversioned meeting to the frozen legacy request contract, so it never calls `/next`; a completed unversioned meeting remains a read-only transcript/summary view. An abandoned meeting of either protocol returns the stable read-only transcript plus clean-join result and performs no generation or database mutation. The deployment procedure transitions the database fence from `legacy` to `draining`, deploys this dual-protocol bridge while new creation is blocked, verifies every serving instance can route both immutable protocols, then changes to `version_1` and immediately reopens stamped creation. Active unversioned rows keep finishing or reach the 24-hour server-clocked abandonment transition on the frozen legacy path; their count does not postpone bridge deployment or new version-1 creation.

For an empty request, load the versioned phase state, owned meeting context from `locals.meetingContext`, the centrally resolved roster, and the compact transcript facts required by `nextMeetingBeat`. Extract the page loader's current deterministic selection into one pure `deriveMeetingRoster({ meetingId, startedAt })` helper using `createSeededRandom(meetingId)` and the meeting record's stable start time. Wrap it in one server-side `resolveMeetingRoster` helper that loads persisted participant rows first and otherwise derives stable seats, calls `ensureCharacterPersistenceIdentities`, then attempts to save participant associations. The page loader and `/next` adopt the resolver here; `/share`, `/close`, and `/expand` adopt it in Milestone 4. If the participant read returns an empty list or a non-not-found seam failure, the resolver may return the derived roster after association save failure only when every character identity was durably resolved first. A unique identity or participant race reloads the canonical rows, which must match the same deterministic selection. An identity failure and a true meeting-not-found result remain recoverable errors rather than exposing a visitor who cannot own a share. Cover the complete join redirect where participant association saving fails after identity success: the page and its first `/next` call show and use identical nonempty seats, and the selected visitor already resolves to a database UUID.

Derive setup crisis only by applying the existing pure `detectCrisisContent` to the owner-checked record's effective stored `userMind`; pass the boolean into the pure engine rather than passing browser text or invoking a model. If setup crisis is true, `intakeCrisisHandled` is false, and no crisis is already active, the engine claims a `crisis_support` beat with `{ source: 'meeting_intake' }` before ordinary opening work. If an active beat exists, return it unchanged and include its canonical cue row for a room cue or canonical spoken outcome for a user gate. If the core chooses a new beat, call `claimNextMeetingBeat` before responding. For a room cue, map the controlled key to its canonical text and pass that cue effect so the active phase and transcript row commit in one transaction. On a version conflict, reload and retry a small bounded number of times; normally the response becomes the beat and optional cue or gate evidence stored by the competing request. If contention does not settle, return HTTP 409 with a retryable seam error rather than choosing locally.

For a room cue acknowledgment, require the matching active cue and its already stored canonical transcript row, then compare-and-set completion without inserting text. A missing or mismatched cue row is a contract failure and leaves phase unchanged. Repeating acknowledgment after advancement returns the stored cue evidence and canonical current beat. For a topic acknowledgment, check `getTopicCompletion` before requiring the active topic gate. An existing result supplies the canonical topic and returns the current active beat, or claims and returns the next beat if the phase advanced before one was stored. With no result, validate against `TOPIC_OPTIONS`, compute completion, and call `completeTopic` so the topic, terminal result, phase state, and version commit together. If two requests submit different valid topics at the same version, only the winner's topic, result, and completed phase may be stored; the loser returns that canonical result rather than its body or a stale-beat error. Prove atomic cue claim, refresh during its presentation pause, cue acknowledgment, differing-topic race, and lost topic-response replay in route and adapter tests. Do not add a standalone topic update.

The returned JSON is a `SeamResult<{ beat: MeetingBeat; phaseState: MeetingPhaseState; effect?: ShareRecord; userGateOutcome?: UserGateOutcome }>` or the repository's equivalent existing response envelope. `effect` is present only for a room cue and is the canonical row persisted before exposure. `userGateOutcome` is present only when the active user gate already has a spoken outcome awaiting analysis; a completed pass has already advanced beyond that beat. The response contains no prompt text, private intake, model context, or arbitrary client instructions. This milestone is complete when repeated empty requests return the same id and cue effect, two simulated claimers converge on one id, persisted and fallback rosters choose the same stable speakers, a failed participant save does not strand the redirected room, a newly claimed room cue never exists without its transcript row, completed room cues advance once, a valid topic survives reload, a protocol-version-1 meeting starts normally, and a legacy mid-round row produces no `/next` work, model call, transcript growth, or phase write. The ownership test proves the endpoint is gated before phase or transcript reads. It satisfies `BEAT-01`, `BEAT-02`, and `BEAT-08` at the route boundary.

### Milestone 4: let specialized routes complete the active beat

After the common ownership gate and strict request parsing, every specialized completion route resolves beat-owned terminal evidence before it requires the beat to remain active. A stored share plus required analysis, completed or skipped generation run, or completed close run can answer a lost-response retry after phase advancement. When the named beat is still active, the route idempotently completes it from that evidence. When phase already advanced, it returns the canonical result without trying to reconstruct the old active beat. Only a request with unfinished work must match the current active beat. The explicitly recoverable partial user-share row resumes through its stored content; any other partially stored invariant that cannot be completed from current canonical state returns a contract error rather than generating a second result.

Migrate `share/+server.ts` first. Normal meeting calls provide only `beatId` plus transport options needed for SSE. The route calls `getShareByBeatId` and reads the terminal generation result before loading active state. If either identifies a completed stored share, return its canonical content and complete only a still-active matching beat. If neither is terminal, require an active `character_share` beat and use `resolveMeetingRoster` to derive the exact `characterId`, voice profile, `interactionType`, phase, selected topic, and sequence position from server state. Ignore no client override silently: reject legacy character or interaction values that disagree so drift is visible during migration.

If no stored share exists, acquire the beat's generation claim. A completed claim reloads its share, an unexpired claim returns a retryable in-progress result with no SSE content, and only the acquired token may call the model. Renew the lease during slow generation and validation. Buffer candidate chunks on the server until the complete candidate passes existing quality checks; do not expose a candidate that may still be rejected. Pass the accepted text, beat id, server-selected character domain id, and validated share metadata to `commitBeatGeneration`. That RPC verifies the current unexpired token, resolves the durable character UUID, inserts the share, updates participant share state, and completes the run together. Only after it returns the canonical stored row may the route compare-and-set beat completion and progressively present that stored text. A stale holder that resumes after lease takeover receives a conflict before insertion and exposes no chunks; a completed or unique-conflict result reloads the canonical row rather than a losing candidate.

If every bounded character candidate fails the quality gate, call `skipCharacterGeneration` with the current claim token, then complete the still-active character beat with a typed skipped outcome and no transcript insertion. Return a stable skipped response so the renderer asks for the next beat without displaying text. If the request fails after recording the skip but before phase completion, a retry loads the terminal skipped run before requiring an active claim, performs the same idempotent completion if that beat is still active, and never calls the model. A retry after advancement returns the same skipped result. Do not use this behavior for crisis support or the empty chair; those beats remain active after a failed generation run.

Migrate `user-share/+server.ts` so, after request validation, it first loads `getUserGateOutcome`. Accept exactly three request shapes: new spoken input `{ beatId, content }`, recovery `{ beatId }`, and a later share-gate pass `{ beatId, action: 'pass' }`. A stored pass is terminal evidence and returns the canonical advanced state without transcript content. A stored spoken outcome identifies the only canonical user share; complete analysis resumes from that row, while a null analysis triggers beat-id-only recovery from its stored content. Recovery never accepts, invents, or rereads browser text. With no outcome, a recovery request fails without mutation. New spoken input must match an active introduction or share gate and calls `claimSpokenUserGate`, which locks the meeting and atomically inserts both the outcome and beat-owned share before returning. Introduction always follows this path with the renderer's `introText()` and rejects a pass. A share pass calls `completeUserGatePass`; that RPC contends for the same gate-outcome key and atomically stores the pass with phase advancement. If pass wins, a concurrent spoken request inserts no share and runs no analysis. If spoken wins, a concurrent pass cannot advance phase and returns the canonical spoken outcome.

For a spoken winner, derive crisis, heavy-disclosure, and significance analysis only from its referenced canonical share, call `setUserShareAnalysisIfAbsent`, and use the canonical winning analysis. Ordinary or crisis phase completion calls `completeSpokenUserGate`, which verifies that the outcome is still `spoken`, names that share, and has the canonical analysis before it atomically stores `phase_version_after` with the meeting phase. Crisis enters `{ source: 'user_share', shareId: canonicalShare.id }` with the interrupted resume point recorded. A losing request's content or proposed analysis cannot affect returned flags or state. A crash after spoken claim but before analysis leaves one recoverable outcome and row; a crash after pass completion leaves no user row. The database lock and shared key make pass/content races deterministic even when the requests begin together.

Migrate `crisis/+server.ts` to accept only `{ beatId }` and first load the beat's completed generation result and stored share. A terminal result returns the canonical support plus the controlled crisis-resource payload after phase advancement. If no terminal result exists, require the active `crisis_support` beat and require `crisisResourcesVisible` to be true before generation. A missing flag is an invariant repair: set it idempotently under the current phase version and return the controlled card even if the later provider call fails. For `user_share`, load that exact owned row and reject a missing, cross-meeting, or non-user record. For `meeting_intake`, load the effective stored `userMind` from `locals.meetingContext` and reject a missing historical value instead of inventing text. Load `userDisplayName` from the same owner-checked context in both cases. Do not accept `userText`, `userName`, a trigger source, or a recent-share guess from the browser.

Check for the support beat's stored share, then acquire its generation claim before any model call. Every response from this point, including in-progress and recoverable failures, carries the controlled resource payload already made durable at crisis entry. A completed claim returns the canonical row and an unexpired claim returns retryable in-progress with no generated content. The holder uses the existing crisis prompt and responder but applies `buildQualityValidationPrompt`, `parseQualityValidation`, and `passesQualityValidationThresholds` to every complete candidate. Require authenticity at least 6, voice consistency at least 6, and all existing exclusions; retry only the existing bounded candidate count. Commit only the first accepted response through token-fenced `commitBeatGeneration`; no ordinary share append may precede that transaction. If the provider fails, every candidate is rejected, or the claim token is stale, mark failure only when the current token still owns the run, leave the same crisis beat active, and return the existing recoverable room error plus resources with no stored or client-visible candidate. A later request may acquire that failed run for an explicit retry.

Completing an accepted support response resumes from the saved pre-crisis state, retains the same typed trigger for any second support beat, then clears it when support ends without resurrecting the interrupted active beat or replaying earlier shares. `crisisResourcesVisible` was already set when the crisis beat began and remains monotonic; intake-triggered completion additionally sets `intakeCrisisHandled` in the same versioned meeting update as phase advancement. Repeated requests return the stored response and controlled resources without generation or validation, and page refresh continues to show those resources from the durable flag.

Migrate `close/+server.ts` so the request contains only the active close beat id. Look up an existing close run before requiring the active beat: return a completed run immediately, return a retryable conflict for an unexpired claim, or acquire/recover the claim. Only the claim holder may use `resolveMeetingRoster`, load context, and run summary generation, memory extraction, callback-candidate discovery, or lifecycle target planning. Load the topic and recent shares on the server instead of accepting them as client authority. Renew the close lease throughout every slow model stage and immediately before checkpoint and finalization; if renewal loses ownership, stop without applying any product mutation.

Add one pure transcript-speaker mapper used by the meeting loader, close context, expansion context, and later renderer. It first checks `isUserShare` and returns the owner-checked display name, then maps `room_cue` and `empty_chair` to a controlled `Room` label, then resolves an ordinary non-null character id from the roster or core profiles, with `Character` as the truthful unknown fallback. Never label a row as the user merely because its character id is null. Close summary, memory, and callback prompt inputs must use this mapper, and route tests must include both new room-owned interaction types.

Refactor callback scanning into candidate discovery with no database write. In `app/src/lib/core/callback-scanner.ts`, extend `CompletedShare` with `isUserShare` and pass the canonical `interactionType` from each stored row instead of rewriting it to `standard`. Add a pure relational validator that receives the exact eligible canonical shares supplied to the scan. For each model candidate, require `originShareId` to name one of those shares from this meeting, require that origin to be a non-user, non-room share with a non-null character, require `characterId` to equal that origin share's domain speaker id, and require normalized `originalText` to occur in the normalized canonical share content. The pure helper uses the same explicit ASCII case and whitespace normalization specified for the SQL finalizer and preserves all other code points including punctuation. Skip an invalid candidate before identity resolution, effect-key assignment, or checkpoint. Batch-resolve every surviving domain character id, including core ids such as `marcus`, through the database identity seam and replace it with a validated UUID in a persistence-only candidate. Missing or ambiguous mappings stop before checkpoint. Refactor lifecycle updates into read-only input loading and pure derivation of explicit target states; each target carries the callback's current `lifecycleVersion` as `expectedLifecycleVersion`. After all model-derived outputs, validated and resolved callback candidates, and targets exist, compute `completeMeetingBeat` with one `completedAt` value and checkpoint the meeting summary, notable moments, endpoint response, persistence-ready candidates with deterministic `<meetingId>:<beatId>:callback:<checkpointed-index>` keys, versioned lifecycle targets, and final phase state before any product mutation. A recovering expired claim reuses the generated outputs, resolved candidates, completion time, and response from the checkpoint. It may reuse lifecycle targets only while their expected versions still match.

Finish only through `finalizeMeetingClose`, which verifies the current token and expected meeting version; deduplicates UUIDs; locks the meeting, close run, sorted origin shares, sorted characters, and sorted lifecycle callbacks in that invariant order; rechecks that each origin belongs to this meeting, its stored speaker UUID equals the callback character UUID, normalized `originalText` still occurs in the locked content, and every expected lifecycle version matches; then atomically writes `endedAt`, summary, notable moments, callbacks, version-incremented lifecycle target states, the checkpointed `finished` phase, and the completed close-run result. A changed origin relationship or content, missing origin, or stale callback version rolls back every write; the version case returns `STALE_CALLBACK_CHECKPOINT`. While the same close token remains current, reload the affected callback inputs, recompute the complete pure lifecycle target set, replace only that checkpoint section with `refreshMeetingCloseLifecycleCheckpoint`, renew the lease, and retry finalization a bounded number of times. Do not regenerate summary, memory, callback candidates, completion time, phase, or endpoint response during that refresh. Do not call `completeMeeting`, callback persistence, lifecycle mutation, or the general phase compare-and-set separately for close. A failed identity, origin-relation, content, or transaction check applies none of those writes. A crash before finalization leaves product state unmodified and the checkpoint recoverable; a crash or lost response after it leaves every mutation committed with a completed run that retries can return without the old active beat. A later retry returns that exact payload with zero generator, discovery, identity resolution, planning, completion, callback, lifecycle, or phase-write calls. If processing fails before finalization, fail the claim with the seam error only when the current token still owns it and leave the meeting explicitly retryable.

Migrate `expand/+server.ts` so the request contains only `shareId`. Verify that the share belongs to the owned meeting, resolve the same persisted-or-fallback roster, then load the persisted topic and bounded recent transcript from the database in stable sequence order. Map every recent row through the same truthful transcript-speaker helper before building the expansion and quality prompts. Keep expansion outside sequence advancement; it must not change the active beat or accept compatibility context supplied by the browser.

Route tests must cover stale ids, wrong beat kinds, abandoned meetings, runtime rejection of room-only character interactions, retries before and after persistence, unique conflicts, quality-rejected candidates, valid share pass, rejected introduction pass, heavy share, both crisis trigger sources, close, expansion, and adapter outages. Lose responses after topic, explicit share pass, character, user-share, crisis-support, and room-moment phase advancement; each retry must return its terminal canonical result without requiring the old active beat, adding transcript rows, or calling a model. The topic retry must return the canonical current beat, and a competing topic body cannot replace the stored winner. Persist `introText()` in ordinary and listening-only flows, reject a direct pass for that beat without changing phase, then let listening-only auto-pass only the later share gates. Race spoken content against pass in both lock orders. Pass-first must produce one pass outcome, no transcript row, no analysis, and one phase advance. Spoken-first must produce one spoken outcome and one share, prevent pass advancement, analyse only that stored text, and schedule crisis support when that canonical text warrants it. Race two character requests and prove one generation claim, one model call, one stored share, identical eventual text, and zero losing SSE chunks; also prove rejected candidates never reach SSE. Reject every candidate for another character beat and prove one terminal skipped run, no share, one phase completion, no regeneration after a lost response, and the same skipped result after refresh. Add a two-request spoken-share race with different text and prove crisis, heavy, significance, response content, and phase outcome all describe the one stored winner. Simulate a crash after the winning spoken outcome and user-share row insert but before analysis or phase completion, then send `{ beatId }` and prove recovery uses that stored content, persists one analysis, advances once, and creates no second share; a recovery request with no owned spoken outcome must fail without mutation. For crisis support, prove both a stored user share and clean-join `userMind` supply canonical text, race two requests to one generation claimant, reject a sub-threshold candidate before accepting the next, and keep the standard resources visible from crisis entry when the provider fails, every candidate fails, the claim is still in progress, a successful response is lost, or the page refreshes. No rejected candidate text may appear or persist. Prove completed intake support does not reappear after refresh. Reload a successfully saved generated visitor and prove `/share` receives its exact three real voice examples and every narrative field. Then make participant association saving fail, select a derived visitor, and prove `/share` generates with that visitor's canonical persisted profile while close and expansion label the same visitor consistently. Add a two-request close race and prove one acquires the claim while the other runs zero effects; keep the first worker healthy past its original expiry through renewal and prove no takeover or repeated model call. Separately recover an abandoned expired lease from its checkpoint. Feed callback discovery canonical `isUserShare` and interaction fields plus a cross-meeting origin id, a valid origin paired with the wrong speaker, and a quote absent from its source; prove all three are skipped before identity resolution and checkpoint. Include whitespace and case variants that pass both the pure and SQL normalizers, plus punctuation changes that remain rejected. Mutate or substitute an origin relation after checkpoint and prove finalization locks it, rejects the complete transaction, and leaves every product row unchanged. Before recovered finalization, reference one lifecycle target from another meeting and prove the stale checkpoint returns no writes, reloads current callback state, refreshes only the lifecycle targets under the claim token, and then commits without losing the newer reference. Race finalizers from two meetings with reversed overlapping callback target arrays and prove the fixed table and sorted UUID lock order yields waiting plus success or a typed stale conflict, never a deadlock. Reject a separate finalization after preparing every mutation and prove meeting fields, callbacks, lifecycle, phase, and run result all remain unchanged; then finalize and lose its response, reload the complete canonical result, and prove every mutation committed together. Use two meetings with the same closing beat and prove their callback keys and rows remain distinct. For close and expansion, include `room_cue` and `empty_chair` rows and prove both prompts label them as `Room`, never as the user. Supply forged expansion topic, transcript, and speaker fields and prove they are rejected while canonical server-loaded context reaches both prompts. Keep the rule that any generated transcript entry with authenticity below 6 or voice consistency below 6 is not shown or persisted; a character is durably skipped, while crisis and empty-chair beats remain active, without completing any beat with rejected text.

This milestone is complete when only the server-selected character, crisis source, responder, and interaction can drive generation; every persisted generated or user entry carries one beat id; entering crisis makes controlled resources durable before provider work and keeps them visible through every generation outcome; a healthy close holder renews ownership; concurrent finalizers cannot reverse lock order; close finalization cannot split any product mutation from its canonical result; and repeating any completion before or after phase advancement returns one canonical outcome. It satisfies `BEAT-03`, `BEAT-06`, and the server half of `BEAT-04`.

### Milestone 5: generate, validate, and persist the empty chair

Add `empty_chair` to `ShareInteractionType` and its database constraint in Milestone 2, then create `app/src/routes/meeting/[id]/room-moment/+server.ts`. Its `POST` body is exactly `{ beatId }`, and it rejects extra client context. After ownership and body validation, check for a stored beat result or completed generation run before loading active state; return that canonical result after phase advancement, or complete it if the same beat remains active. Only unfinished work requires the matching active `generated_room_moment` with `moment: 'empty_chair'`. Then use the same generation-claim contract as character shares so exactly one request can invoke the model; competing requests receive in-progress or completed canonical behavior. Call the existing Grok seam with a stable internal correlation character id such as `empty-chair-room`. Before persistence, validate every candidate with a new pure `buildRoomMomentQualityValidationPrompt` that returns the existing `QualityValidationResult` schema. For this non-character entry, `voiceConsistency` means consistency with the room voice and style constitution. Accept only candidates for which `passesQualityValidationThresholds` enforces authenticity at least 6, voice consistency at least 6, and the existing therapy, moralizing, generic-language, and emotion-labeling exclusions.

Use the same bounded candidate-retry shape as the existing share path. Commit the first accepted result through `commitBeatGeneration` with `characterId: null`, `isUserShare: false`, `interactionType: 'empty_chair'`, and the active beat id; the share and completed run must commit under the current token before compare-and-set completion of that beat. The client-facing transcript mapper renders this interaction type as a ritual or room entry with no character speaker label. Do not treat null character id as the user for this entry. A retry checks the run and `getShareByBeatId` first, completes a still-active matching beat if needed, and returns the canonical stored text without generation or validation. A stale holder after takeover inserts nothing, while a completed or unique-conflict result reloads the canonical row and follows the same completion path.

The generation prompt must ask for a brief, naturally complete empty-chair moment with no names, therapy language, slogans, forced physical action, explained moral, or exact sentence count. It must omit empty sections rather than render placeholder values. Preserve the style constitution. The hardcoded sentence in `runFreshMeeting` must not survive as a fallback. A generation or validation failure shows the existing recoverable room error, leaves the same beat active, and persists nothing.

Update prompt tests, generated-room route tests, transcript mapping tests, and the integration sequence. The mock response should be clearly written test content rather than `example line` placeholders. Prove first-candidate rejection followed by acceptance, all candidates rejected, provider failure, one generation on successful first completion, zero generation and validation calls when the stored beat is retried, one persisted entry, no speaker label, and the same text after refresh.

This milestone is complete when two newly created meetings may receive different accepted outputs, one meeting always keeps its own first persisted output, rejected output never appears or completes the beat, and no literal production fallback supplies the old sentence. It satisfies `BEAT-07` and closes #83.

### Milestone 6: replace the browser script with one renderer loop

Use the database creation fence for this cutover. Land the additive migration, trigger, activity expiry, and dual-protocol application while the singleton remains `legacy`, so the old application can create only unstamped meetings. The bridge freezes the current `runRound*` route contract and renderer for unversioned rows and adds the beat route contract and generic renderer for version-1 rows; the server-loaded immutable meeting protocol selects between them, never the browser. When the deployment window begins, compare-and-set the mode to `draining`; every old and new application instance then receives the recoverable join-unavailable result for creation while existing meetings continue on their current renderer. Keep creation blocked only until every serving instance is bridge-capable and has the trusted version stamp, then compare-and-set to `version_1` and reopen stamped creation. Active unversioned meetings continue on the frozen legacy path until they complete or a 24-hour server-clocked inactivity transition marks them abandoned. In `version_1`, the trigger rejects an unstamped insert from any stale instance. Update `app/src/routes/meeting/[id]/+page.server.ts` to expose `initialSummary` from the authorized meeting record's nullable stored summary and `initialCrisisResources` from `crisisResourcesVisible` using the single controlled payload. Select the renderer from the authorized meeting's immutable protocol: version 1 starts the generic loop, a non-abandoned unversioned meeting continues through the frozen legacy renderer, a completed meeting renders its stored transcript and summary, and an abandoned meeting renders its stored transcript read-only with a clean join link. Add creation and loader tests for all three fence modes, a late unstamped insert after activation, concurrent legacy and version-1 meetings on their own renderers, abandonment, and a post-meeting refresh after the close response was lost.

In `app/src/routes/meeting/[id]/+page.svelte`, preserve the existing transcript components, SSE preview presentation, input components, crisis display, and reflection. Initialize `summaryText` from `initialSummary` and the sticky crisis card from `initialCrisisResources`; crisis entry keeps that controlled card visible independently of model success or the current active beat. Keep the existing orchestration functions and their route request shapes frozen behind the unversioned branch during the bridge. After `BEAT-S` proves zero active non-abandoned unversioned meetings, delete `runFreshMeeting`, `runRounds`, `runRoundOne`, `runRoundTwo`, `runRoundThree`, `runClosing`, `continueFromPersistedPhase`, and their speaker-picking, hard-question, round-specific random, and phase-replay helpers.

Write one small loop that posts to `/next`, switches on the returned discriminated beat kind, and renders it. A `room_cue` response includes the canonical cue row already stored with the active beat. Upsert that row by share id before display, map its controlled key to the existing system, action, or ritual presentation, wait only the remaining portion of `pauseAfterMs` measured from the row's stored `createdAt` and clamped to zero through the full pause, then acknowledge that id. A refresh during the pause rehydrates and upserts the same row, does not replay the reveal animation, and immediately acknowledges when the canonical pause has already elapsed. Acknowledgment never inserts the cue. `character_share` requests the server-selected beat through `/share`; accepted canonical stored text may still be chunked for progressive display after validation and persistence, while a canonical skipped result adds no transcript entry and immediately asks for the next beat. `generated_room_moment` posts its id to the validated `/room-moment` path completed in Milestone 5. `user_gate:introduction` retains the shipped introduction control and posts `introText()` as content; it has no pass action, including in listening-only mode. `user_gate:topic` presents topic choice. For either spoken `user_gate`, the `/next` response first checks the active beat's canonical `userGateOutcome`. If it is spoken, hide the input immediately and post `{ beatId }` to `/user-share`; that content-free request completes or resumes analysis from the referenced stored row. With no outcome, `user_gate:share` presents the later sharing control or automatically posts the controlled pass only when listening-only applies. Each gate stops the loop until its valid completion. `crisis_support` posts its id to `/crisis`. `close_meeting` invokes `/close`. `finished` renders the existing reflection with `summaryText`, including `initialSummary` after refresh, and stops.

For character, crisis-support, room-moment, and close routes, treat a valid `in_progress` result as a claim state rather than the existing terminal error presentation. Re-request the same beat id after the server's validated `Retry-After`, capping each delay at 5 seconds and stopping after 30 seconds total. Every delay and request shares the renderer's cancellation token and stops on unmount, navigation, or crisis interruption. Success or a terminal skipped-character result resumes the loop. Exhaustion shows one recoverable retry control bound to the same beat; it does not call `/next`, advance locally, or start a second sequence. Malformed or missing `Retry-After` uses a documented 1-second default. Component tests return in-progress twice then canonical completion, cover the bounded stop, and prove cancellation starts no later request.

The page may own animation, waiting, and bounded claim-retry mechanics, but it must not own phase-specific delays or decisions. The `pauseAfterMs` value comes from the beat. The client may clamp an unreasonable value to a documented safe presentation range, but must not substitute a different sequence. It may upsert transcript entries by stored share id; it must not invent persisted sequence order or append a second local copy. Use the shared transcript-speaker mapper so `isUserShare` wins, `room_cue` and `empty_chair` remain room-owned, and neither null-character room entry receives a user or character speaker label.

Move `TOPIC_OPTIONS` to the shared pure module and import it for rendering. Topic selection acknowledges the active topic gate through `/next`. User introduction and round shares go through `/user-share` with their beat ids. Remove private meeting context from generation query strings where the server can derive it from `locals.meetingContext` and stored state.

Use loader, component, or route-backed Playwright tests to prove each beat kind and a skipped character render through the generic loop, in-progress claims retry and converge without duplicate output, a refresh during a cue pause reuses its stored row and remaining delay without a second reveal, an active spoken user gate with a stored but unanalyzed outcome hides its input and posts exactly one beat-id recovery, crisis resources appear as soon as crisis starts and survive provider failure, phase advancement, and refresh, and `finished` after reload displays the stored close summary. During the bridge, prove a non-abandoned unversioned meeting continues only through the frozen legacy renderer and never calls `/next`, a version-1 meeting uses only the beat renderer, and an abandoned row renders read-only transcript plus restart. Test that `draining` rejects every creation during bridge deployment and that `version_1` rejects an unstamped insert from a simulated stale instance while allowing new stamped rooms before legacy activity reaches zero. After the zero-active non-abandoned legacy gate, a source assertion may supplement behavior by rejecting reintroduction of the named `runRound*` and `continueFromPersistedPhase` functions, but behavior tests remain the primary proof. This milestone is complete when the version-1 path has no hardcoded speaker order, optional branch selection, round recursion, phase replay, duplicate user solicitation after partial persistence, or terminal mishandling of a valid in-progress claim, and each immutable protocol stays on its assigned renderer. It satisfies `BEAT-04` and the reflection portion of `BEAT-05`.

### Milestone 7: prove replay-free refresh

Expand `app/src/lib/server/routes/meeting-ritual-phase.integration.spec.ts` or create a focused integration suite that drives the next-beat protocol through every phase using the mock database. At each point after a generated share, skipped character, newly exposed and acknowledged room cue, and user gate, snapshot the persisted transcript, generation or gate outcome, roster, and state, reconstruct the route/page inputs as a refresh would, ask for the next beat, and assert that entry ids, cue keys, skip result, share count, and prior content are unchanged. Refresh once after cue exposure but before its pause or acknowledgment finishes; require the same active beat, stored cue row, no second reveal, the remaining delay derived from canonical `createdAt`, and one eventual acknowledgment. Then complete the returned beat and continue. Force participant saving to fail after join and prove the loader and `/next` independently derive the same seats; carry the derived visitor into `/share`, close, and expansion and prove the voice profile and label remain identical. Include the shipped crosstalk branch where one character takes the following scheduled share and prove both beat ids complete while the unique participation list contains one id.

Extend the existing `createGrokAiMock` with a fixture-backed scripted scenario selected only when the server test composition is constructed. Classify calls by stable correlation id and controlled prompt marker, then return deterministic accepted character, crisis-support, and empty-chair prose; valid quality JSON above both minimum scores; a close summary; memory output; and callback-scan output as the real routes require. Record calls by meeting and prompt category for unit and integration assertions. Do not select output through a browser header, cookie, query parameter, or test-only HTTP endpoint. Install this mock as `event.locals.grokAi` through the `E2E_MOCK_SEAMS=1` composition established by the privacy plan, with `reuseExistingServer: false`; no route-backed browser case may construct or contact the real Grok adapter.

Add Playwright coverage in `app/tests/e2e/meeting-flow.spec.ts` for at least a mid-round refresh, a refresh while waiting for the user, a refresh after the user's spoken outcome and beat-owned share persisted but before analysis or phase completion, a refresh during a room-cue pause, a refresh while another request owns an in-progress claim, and a refresh immediately after a generated or skipped response. Capture the transcript entry ids before refresh and compare after rehydration. No id may disappear, change content, or appear twice, a cue reveal must not replay, and a skipped beat must stay absent without regenerating. At the partial user-share boundary, the stored outcome and row must remain visible, the input must stay hidden, one `{ beatId }` recovery must finish analysis and phase advancement, and the user must not be asked to repeat the text. The in-progress page must poll the same beat according to `Retry-After` and eventually render or skip its canonical result. The next new entry must have the next beat id and sequence order. Seed a historical mid-round meeting with null protocol version and prove it continues only through the frozen legacy path, while a newly created version-1 meeting begins the generic loop. Advance the historical row past 24 hours without a successful mutation, run expiry, and prove it changes to the read-only restart presentation without fake completion; race expiry against a real server mutation and prove exactly one wins.

Exercise clean-join crisis from stored intake and prove the resource card appears when that beat is claimed, before a support model call. Refresh during support, complete support, and prove it does not trigger again. Separately exercise crisis during a user gate and prove `completeSpokenUserGate` makes the card durable in the same transaction that enters crisis. Make the provider fail and make every bounded candidate fail quality; in both cases no generated text appears, the beat remains retryable, and resources stay visible through refresh. Complete support and verify the room resumes from the stored point. Lose the accepted support response after phase advancement, refresh, and prove the standard resources remain sticky from `crisisResourcesVisible`. Exercise simulated network loss after character, user-share, crisis-support, and room-moment phase advancement; each retry must return its stored terminal outcome without the old active beat, transcript growth, or a model call. Exercise a direct close retry after its atomic finalization response is lost and assert that the stored close result returns without any repeated summary, memory, callback, lifecycle, or phase work. Separately refresh the page after close advanced to `finished` and prove the loader's stored `initialSummary` renders the same reflection without needing the cleared close beat id. Hold close generation beyond its first lease while renewing and prove a competitor never takes over; separately abandon a checkpointed claim and prove takeover reuses the checkpoint.

Include room cues and the empty-chair row in the server-built close and expansion contexts and assert that both are labeled `Room`, while the real user row alone uses the person's display name. Return callback candidates with a cross-meeting origin, mismatched origin speaker, and absent quoted text and prove none reaches checkpoint; then change a valid origin relation before finalization and prove its locked recheck rolls back the whole close. Start two meeting finalizers whose checkpoints name overlapping callback targets in reversed order and prove one waits without deadlock, then commits or returns the typed stale-version conflict. Reject one additional close finalization and assert byte-identical meeting completion fields, callbacks, lifecycle state, phase, and run result; then retry and prove the full set commits together. Rehearse creation while `draining` during bridge deployment and prove the database rejects it; then activate `version_1`, prove stamped creation reopens while a legacy row remains active on its frozen path, and prove expiry eventually removes an abandoned row from the active count. Remove the 2026-03-19 replay-free refresh item from `DEFERRED.md` only after these cases pass. If any phase cannot be proven, keep the item and record the exact phase and state rather than declaring #82 solved. This milestone is complete when refresh alone never starts duplicate generation, never increases transcript length, never repeats a persisted user prompt, preserves crisis resources from entry through every provider outcome, and automatically converges on partial, terminal, or in-progress canonical outcomes. It satisfies `BEAT-05` and `BEAT-06`.

## Concrete Steps

Run all application commands from `C:\Users\shiva\OneDrive\Documents\ChatGPT\14thstep\app` in PowerShell, using `npm.cmd` so PowerShell execution-policy shims do not interfere.

After Milestone 1:

    npm.cmd run test:unit -- --run src/lib/core/meeting-beats.spec.ts src/lib/core/ritual-orchestration.spec.ts src/lib/core/prompt-templates.spec.ts src/lib/core/meeting.spec.ts
    npm.cmd run verify:core
    npm.cmd run check
    rg -n "new Date\(|Date\.now\(" src/lib/core/meeting-beats.ts src/lib/core/ritual-orchestration.ts
    rg -n -i --pcre2 "(?:\b(?:one|two|three|four|five|six|seven|eight|nine|ten)-sentence\b|(?:\b[0-9]+\b|\b(?:one|two|three|four|five|six|seven|eight|nine|ten)\b)(?:\s*-\s*(?:[0-9]+|one|two|three|four|five|six|seven|eight|nine|ten))?(?:\s+[\w-]+){0,3}\s+sentences?)" src/lib/core/prompt-templates.ts src/lib/core/meeting.ts
    rg -n -F "None yet" src/lib/core/prompt-templates.ts src/lib/core/meeting.ts
    rg -n -F "No callbacks referenced" src/lib/core/prompt-templates.ts src/lib/core/meeting.ts
    rg -n -i --pcre2 "(?:section\s*:\s*none|section\s*:\s*n/a)" src/lib/core/prompt-templates.ts src/lib/core/meeting.ts

Expect the named suites and core verification to pass and `svelte-check` to report zero errors. Expect every source search to return no matches after each touched ritual helper accepts its timestamp as an argument, every generation prompt in `prompt-templates.ts`, including crosstalk, plus the close-summary prompt uses natural brevity guidance without numeric or worded sentence counts, and optional prompt inputs omit their full section rather than emitting a placeholder. Unit cases must still prove nonempty recent-share and callback sections render normally.

After Milestone 2:

    # Precondition: the merged #79 migration and adapter changes leave one row per immutable core slug and no read-time core insert.
    npm.cmd exec supabase start
    npm.cmd exec supabase db reset
    npm.cmd run probe:supabase-meeting-beats
    npm.cmd run test:unit -- --run src/lib/seams/database/contract.test.ts src/lib/server/seams/database/adapter.spec.ts
    npm.cmd run verify:contracts
    npm.cmd run check

Expect the precondition and real local probe to prove one canonical core UUID per slug with zero read-time creation; staged activity backfill with no nulls and a database default accepted by the unchanged legacy insert shape; deletion of a parent meeting cascading through topic, user-gate, generation, and close-run rows; adjacent, compare-and-set creation-fence transitions; complete insert rejection in `draining`; stamped-only insertion in `version_1`; immutable protocol routing through the bridge; idempotent 24-hour abandonment with transcript preservation; mutation-versus-expiry serialization and server-owned activity updates; idempotent visitor UUID and full-profile resolution before participant association; same-second cross-meeting isolation; successful participant reload with the required voice tuple; callback domain-to-UUID mapping; rejection of cross-meeting origin, mismatched speaker, and absent quote candidates; lifecycle-version increments; rejection and zero writes from a stale callback checkpoint or invalid locked origin relation; token-checked lifecycle recomputation; reversed overlapping callback targets without deadlock; one version winner with atomic pre-generation crisis-resource visibility; atomic topic completion and replay; atomic cue beat plus transcript claim; spoken-versus-pass serialization in both lock orders; introduction-pass rejection; one generation claimant with renewal; one token-fenced share/run commit; stale-token rejection after takeover; one character-only skipped result; rejection of invalid skip kinds; one canonical user analysis plus content-free recovery of an unanalyzed spoken outcome; one close claimant; meeting-scoped callback keys; domain-id checkpoint rejection; token rejection; healthy slow-holder renewal; full finalization rollback; the jointly stored meeting fields, callbacks, lifecycle targets, finished phase, and completed result; completed replay; and abandoned-lease recovery before fixture, mock, contract, and adapter checks pass. If #79 is not merged or the real probe cannot run, stop the database-dependent milestones and record the blocker. Do not apply the migration to the unavailable production tenant as part of this local gate.

After Milestones 3 and 4:

    npm.cmd run test:unit -- --run src/lib/server/routes/meeting-next-beat.spec.ts src/lib/server/routes/meeting-share.spec.ts src/lib/server/routes/meeting-user-share.spec.ts src/lib/server/routes/meeting-crisis.spec.ts src/lib/server/routes/meeting-close.spec.ts src/lib/server/routes/meeting-expand.spec.ts src/lib/server/routes/meeting-ritual-phase.integration.spec.ts
    npm.cmd run verify:composition
    npm.cmd run check

Expect all next, bridge-routed protocol cutover, abandonment, atomic topic completion, atomic cue claim, spoken-versus-pass user-gate serialization, introduction-pass rejection, fallback-roster, complete persisted visitor-profile, durable fallback-visitor identity, repeated-speaker, cue-pause refresh, token-fenced generation, stale-holder takeover, terminal-result-first completion, partial user-share recovery, retry, character-interaction validation, intake and user-share crisis, resource visibility before model success, callback transcript relation and persistence-id, lifecycle-version conflict and refresh, deterministic finalizer lock order, close-renewal, truthful transcript-label, and expansion cases to pass. Character and crisis races must expose no losing or rejected text, while crisis provider and validation failure must still expose the controlled resource card. The user-share test must recover a spoken outcome inserted before analysis using only its beat id and must not create a second row. The pass race must never leave both a transcript row and an advanced pass state. The close test must keep identical beat ids in different meetings isolated, reject invalid candidate relationships before checkpoint, map valid callback domain ids, reject a changed locked origin relation or lifecycle target without applying any product mutation, recompute a stale lifecycle target from the newer row under the current token, prove reversed overlapping callback target arrays cannot deadlock, and prove every product mutation and the run result then finalize atomically. The composition check must continue to prove that database and model I/O stay in server modules.

After Milestones 5 through 7:

    npm.cmd run test:unit -- --run src/lib/core/meeting-beats.spec.ts src/lib/core/prompt-templates.spec.ts src/lib/server/routes/meeting-generated-room-moment.spec.ts src/lib/server/routes/meeting-ritual-phase.integration.spec.ts
    npm.cmd run test:e2e
    npm.cmd run lint:verify
    npm.cmd run check

Expect the full browser flow, required persisted introduction in listening-only mode, rejected introduction pass, database-fenced bridge deployment and version-1 activation, simultaneous immutable legacy/version-1 rendering, abandoned read-only restart, skipped-character case, successful persisted-visitor profile reload, failed-roster-association with durable fallback visitor, repeated-speaker case, in-progress claim retry, stale-generation-holder rejection, partial user-share refresh recovery, lost-response terminal retries, crisis resources through provider failure, callback candidate-relation validation and UUID resolution, reversed-overlap close contention, slow close renewal, truthful room labels, and dedicated refresh cases to pass against the server-process auth, database, and scripted Grok mocks with no provider calls. After the zero-active non-abandoned legacy cleanup in `BEAT-S`, check that the page no longer contains the retired sequence functions:

    rg -n "runFreshMeeting|runRoundOne|runRoundTwo|runRoundThree|runClosing|continueFromPersistedPhase" "src/routes/meeting/[id]/+page.svelte"

Expect no matches. Then run every nonfixture repository gate:

    npm.cmd run verify:contracts
    npm.cmd run verify:core
    npm.cmd run verify:composition
    npm.cmd run test:e2e

Finally run `npm.cmd run verify`. Until #71 and #91 are resolved, expect failure only at `verify:fixtures` because the provider fixtures are stale. All earlier checks must pass, and any other failure must be fixed before completion.

Rehearse the forward cutover on the disposable local stack. Start in `legacy`, create one unstamped active meeting, and compare-and-set the singleton to `draining`. Verify every stamped and unstamped insert now receives the stable recoverable fence error. Deploy the bridge and confirm every serving instance can keep that row on the frozen legacy renderer while routing version-1 rows to the beat renderer. Transition to `version_1` without waiting for the legacy row to finish and prove a stamped insert succeeds while an unstamped insert from a stale instance fails. Drive both meetings concurrently and prove each stays on its immutable route and renderer. Advance the legacy row's server-owned activity on a successful mutation, then set it beyond the 24-hour cutoff, run bounded expiry, and prove `abandoned_at` is set without `ended_at`, summary, or transcript fabrication. The zero-active-unversioned cleanup query excludes abandoned rows and permits removal of the frozen legacy path only after every remaining unversioned row is completed or abandoned.

For the symmetric rollback rehearsal, compare-and-set `version_1` to `draining` while keeping the beat renderer and specialized routes deployed. Verify all new meeting creation is fenced, deploy a bridge that routes existing version-1 meetings to that renderer and new unversioned meetings to the frozen legacy renderer, verify every serving instance, then compare-and-set to `legacy` and reopen unversioned creation. Existing version-1 rows continue or expire on the beat renderer. Before removing it, query the target database:

    select count(*) as active_version_1_meetings
    from public.meetings
    where meeting_protocol_version = 1
      and ended_at is null
      and abandoned_at is null;

Expect zero only before removing the beat renderer or specialized completion routes, not before reopening legacy creation. Seed one active version-1 row and prove the bridge allows an unversioned meeting to start after `legacy` activation while that row remains on the beat renderer. Finish or safely expire the version-1 row, rerun the query, and only then remove the version-1 branches. Prove the reopened database accepts an unstamped meeting and rejects a stamped one. If a version-1 row remains active, keep its renderer and routes deployed while legacy creation continues through the bridge.

From the repository root, inspect the final change:

    git diff --check
    git status --short

Expect no credentials, environment files, generated test output, or unrelated production changes.

## Validation and Acceptance

Start from a new owned meeting created through the clean join flow. Record each returned beat id from opening through post-meeting. A second request made before completing any beat must return the identical id and payload. Completing a beat twice must produce one stored effect and one next state.

First save and reload a generated visitor through participant persistence. Require the reloaded profile to retain every original narrative field and exactly the same three real voice examples, then generate a share using that canonical stored profile. Repeat the join with participant association persistence failing and no participant rows. The resolver must first insert or load each derived visitor by its meeting-scoped stable identity key and return the same database UUID and complete stored profile on repeat. Start a second meeting in the same second and prove an equal visitor slot or domain id cannot alias the first meeting's generated profile or UUID. The page loader and `/next` must derive the same ordered roster from the meeting id and stored start time, the room must reach its first character beat, and refresh must preserve those speakers. `/share` must use the selected fallback visitor's full canonical profile and atomically persist against that durable UUID, while close and expansion use the same visitor name. Reject an incomplete profile or placeholder voice example before selection. Fail identity creation itself and prove the resolver returns a recoverable error before selecting the visitor. A real meeting-not-found response must still fail rather than manufacture seats.

During each sharing round, refresh after the first character, while waiting for the user, after the spoken outcome and user-share row persist but before analysis, and immediately after completing the user share. Before and after refresh, transcript ids and content must match exactly. Refresh at the partial boundary must hide the input and automatically send only the beat id; recovery analyses the canonical stored text, advances once, and never asks for or inserts the share again. Refresh must not call the share generator. The next new beat must follow the saved cursor, not the beginning of the round. Race a pass against crisis-bearing spoken content with pass first and with spoken first. Each race must preserve one gate outcome: pass first advances with no transcript or crisis work, while spoken first stores one share, prevents pass advancement, and schedules support from that canonical row. Lose explicit and listening-only pass responses, then require the stored gate outcome without a stale-beat error or invented transcript row. Attempt that same pass on the introduction gate and require rejection with unchanged phase and no outcome row; complete it with the canonical persisted `introText()` in both ordinary and listening-only modes. Lose character and user-share responses after their phase updates, then retry their ids and require the canonical stored results without an active-beat error, transcript growth, analysis drift, or model call. Force the optional crosstalk speaker to take the following scheduled share; both distinct beat ids must complete, the cursor must advance twice, and the unique participation set must contain that character once.

Lose a topic-acknowledgment response after its transaction commits. Retrying that id with either the same or a different valid topic must load the stored topic outcome and return the canonical current beat without changing topic or phase again. For an opening cue and the closing ritual, verify the active cue and canonical transcript row commit before `/next` returns. Refresh during each presentation pause; the same stored row, controlled key, rendered text, and id must reappear exactly once, the reveal must not replay, the client waits only the remaining canonical delay, and acknowledgment performs no insert. Close and expansion prompt captures must label those cues and the empty-chair entry as `Room`; only rows with `isUserShare` may use the person's name. Race two character-generation requests for one active beat: only the claim holder may receive generated chunks, and both callers must eventually converge on the same stored accepted text. Let the first holder expire, let a replacement acquire, then resume the stale holder before the replacement commits; the stale token must insert no share, update no participant state, and emit no chunks, while the replacement atomically commits its share and completed run. Construct character beats with `room_cue` and `empty_chair` interaction values and prove both type validation paths reject them before generation.

Reject every bounded candidate for another character. The run must become terminally skipped with `quality_rejected`, the beat must advance without a transcript row, and a lost response or refresh must return the same skip without another model call. Attempting that skip for crisis support or the empty chair must fail and leave the beat active. While a different request holds any generation or close lease, return `in_progress` twice and then its canonical result; the renderer must honor bounded `Retry-After` delays, request the same beat, and resume automatically. Exhaustion exposes one retry control for that beat, and navigation cancels every pending delay and request.

For two different meeting ids with the same roster and intake, optional choices may differ. Repeating either meeting from its saved state must not differ. Every selected character must belong to the persisted roster. Every interaction type must match the active beat. No browser-supplied character, phase, sequence order, topic, recent transcript, or interaction override may control server behavior. Expansion accepts a share id and derives both prompt and quality context from the owned meeting and stored shares.

Start one meeting with crisis language in the clean join intake. Its support beat must carry `{ source: 'meeting_intake' }`, load text from the owner-checked meeting record, survive refresh, and set `intakeCrisisHandled` when support finishes so the same intake does not trigger again. In another meeting, trigger crisis during a user gate. The ordinary loop must stop, the crisis-support beat must store `{ source: 'user_share', shareId }`, and refresh must remain at that support beat. Submit altered `userText`, `userName`, trigger source, or share id to the crisis endpoint and prove they cannot control its prompt.

Race two support requests and prove one generation claimant calls the model. The controlled resource card must already be visible from crisis entry. Reject one sub-threshold response before accepting a response whose authenticity and voice-consistency scores are both at least 6; only the accepted canonical text may persist or appear. When every bounded candidate is rejected or the provider fails, no model text persists or appears, the same beat remains active for retry, and the resource card remains visible before and after refresh. Completing user-share-triggered support must resume from the pre-crisis point without replaying earlier beats; it preserves the already monotonic `crisisResourcesVisible` flag. Lose the response after advancement, retry without the old active beat, and refresh; both paths return the canonical support and the sticky controlled resources without generation. Race the original user-share payload against different non-crisis text for the same beat and prove every returned flag and state transition describes whichever share and analysis the database preserved.

At the empty-chair beat, the model receives the pure empty-chair prompt and every candidate receives the room-moment quality prompt. The transcript stores one null-character `empty_chair` entry only after authenticity and voice consistency both reach 6 and all existing exclusions pass. It displays without a person's name, remains identical after refresh, and contains none of the forbidden placeholder or forced-count instructions. Lose the response after phase advancement and prove a retry returns that row without requiring the old active beat or generating again. If generation fails or all candidates are rejected, the same active beat remains available for retry, persistence remains unchanged, and no hardcoded replacement appears.

Race two close requests for the same beat. Exactly one may run summary, memory, callback discovery and identity resolution, lifecycle planning, and finalization; the other receives the documented in-progress response. Keep that holder healthy beyond the original lease through token-checked renewal and prove the competitor cannot take over or repeat a model call. Give candidate discovery an origin from another meeting, a candidate whose character differs from the origin speaker, and quoted text absent from the origin content; all three must be skipped before identity resolution or checkpoint. Discover a valid callback for domain id `marcus`, resolve it to the one core UUID established by #79 before checkpoint, and prove the final callback row references that character and its canonical origin share. A missing or duplicate core mapping and a raw domain-id checkpoint must both fail before product mutation. Change a checkpointed origin's meeting or speaker relation before finalization and prove the locked recheck rolls back the complete transaction. After the winner completes, every retry must return its stored canonical response without invoking any preparation or mutation. Separately abandon a claimed worker after it checkpointed output. Before recovery finalizes, have a different meeting reference one target callback and increment its lifecycle version. The stale checkpoint must return `STALE_CALLBACK_CHECKPOINT` and leave meeting fields, callback inserts, lifecycle state, phase, and close-run result unchanged. The recovered holder reloads callback inputs, derives targets from the new version, replaces only the lifecycle section under its current token, and then finalizes without discarding that intervening reference or rerunning model work. Start finalizers for two meetings whose checkpoints list at least two shared callback targets in opposite order; explicit table order and `ORDER BY id FOR UPDATE` must produce waiting followed by a successful commit or `STALE_CALLBACK_CHECKPOINT`, with no deadlock error. Force a separate finalization to reject and prove the same full rollback; then retry successfully and prove the complete set commits in one transaction. Lose that response and prove rereading the completed run returns the canonical payload even though the close beat is no longer active. Repeat the same deterministic beat id in a second meeting and prove meeting-scoped callback keys cannot collide. Refresh after phase advancement and prove the stored summary still renders in reflection.

Seed an in-progress historical meeting whose phase JSON represents the middle of a round and whose `meetingProtocolVersion` is null. During the bridge, the loader must keep it on the frozen legacy renderer and direct `/next` access must reject it without inferring cursor zero or writing beat state. A completed historical meeting may still show its stored transcript and summary. Transition the database fence to `draining`, deploy the bridge, activate `version_1` while that historical row remains active, and prove trusted creation enters the generic renderer while a stale unstamped creator remains rejected. Prove the two meetings continue only on their assigned renderers. Advance the historical row beyond the 24-hour cutoff, expire it under a server row lock, and prove it then presents the stored transcript read-only plus a clean join link without `ended_at` or fabricated summary. The legacy renderer may be deleted only when the active unversioned query, excluding abandoned rows, reaches zero.

The final page must feel continuous. The user sees the existing room pacing and one relevant control at a time; no phase label, beat id, cursor, retry status, or orchestration vocabulary is shown in the product UI.

## Idempotence and Recovery

Beat ids are stable functions of persisted inputs. Claiming an already active beat is a read; selecting a new cue atomically writes both its active phase and transcript row before response. After ownership and request validation, terminal share, analysis, topic, user-gate, generation, skip, or close evidence is checked before active state, so a completed lost-response retry returns its canonical result even after the phase moved on. A spoken outcome whose share analysis is null is a deliberate recoverable state: hydration sends its beat id without content, the route reloads the referenced canonical text, and first-analysis-wins plus phase compare-and-set makes original and recovery requests converge. Topic completion enters its narrow ledger with topic and phase. Spoken and pass submissions serialize through one user-gate key: the first stores either a share reference or an advanced pass, and the loser returns it without applying another effect. Introduction always stores a spoken outcome and can never pass. Cue acknowledgment advances the pre-persisted row and never inserts it. Character, crisis-support, and room-moment routes acquire a leased generation-run row before any model call and keep candidates server-buffered through quality validation. The current unexpired token fences the transaction that inserts the share, updates participant state, and completes the run, so an expired holder cannot win through the unique index after takeover. A fully rejected character records a terminal skipped run before phase completion; recovery can complete that same beat without a share or model call. The database rejects skipped outcomes for crisis and room-moment runs. Entering a crisis beat sets the monotonic resource-visibility flag before generation, so refresh and every provider or validation outcome reconstruct the controlled card independently of generated support.

Compare-and-set phase updates never overwrite a newer version. A losing next-beat request reloads and returns the winner's beat. A losing completion request reloads, checks whether its beat already completed, and returns the canonical outcome or a clear stale-beat conflict. Bound retry loops; do not recurse indefinitely under contention.

The page loader, `/next`, `/share`, `/close`, and `/expand` call one server roster resolver backed by one pure derivation with stable meeting inputs. That resolver idempotently establishes every visitor UUID and complete immutable profile before returning fallback seats, and persisted participant reads rehydrate that same validated snapshot. A failed participant association cannot produce an unpersistable or incomplete speaker, different voice profile, or transcript label. A repeated character across two planned beats updates the unique participation set idempotently while each beat id advances the cursor once. An in-progress lease never advances client state; bounded retries request only that same beat until they receive its completed or skipped outcome or present a recoverable stop. Unversioned state is never passed to the beat engine; the bridge keeps a non-abandoned row on frozen legacy behavior. Expiry uses the server clock and meeting row lock, rechecks inactivity, sets only `abandoned_at`, and turns either protocol into a read-only transcript plus restart. Every successful route mutation updates `last_activity_at` in the same transaction, so it cannot race into abandonment.

Close has a second, durable claim because its work begins before phase completion. The `(meeting_id, beat_id)` close-run row selects one worker, renews that ownership during slow preparation, and preserves generated checkpoints across lease recovery. Callback effect keys include both meeting and beat. Before checkpoint, the pure scanner validator binds every candidate to an eligible origin in the exact scanned meeting transcript, that origin's speaker, and text contained in that share. Callback targets are then resolved to validated database UUIDs. Finalization deduplicates UUIDs and acquires locks in the invariant meeting, close-run, sorted origin-share, sorted character, sorted callback order before it rechecks relationships and versions. Meeting completion reuses one checkpointed completion time, and lifecycle work produces explicit target states paired with expected callback lifecycle versions. Every ordinary callback reference or state mutation increments that version. The final RPC aborts the entire transaction when an origin relationship is invalid or with `STALE_CALLBACK_CHECKPOINT` when any lifecycle version changed. The current token holder then reloads callbacks, recomputes and replaces only the lifecycle target section, and retries within a bound; summary, memory, candidates, completion time, phase, and response remain canonical. A valid final RPC applies meeting completion fields, callbacks, version-incremented lifecycle targets, the checkpointed `finished` phase, and the completed canonical response in one transaction. A failed transaction changes none of them; a lost response finds all of them committed. The completed close run and meeting summary remain authoritative after the active beat has cleared.

Apply the migration only after the private-intake migration and the merged #79 core-identity migration. Its added columns, tables, constraints, indexes, singleton, and trigger are safe to create twice. If code rollback becomes necessary after a live migration, keep nullable `characters.source_identity_key`, `characters.source_profile`, `shares.beat_id`, `shares.user_analysis`, `shares.room_cue`, `meetings.phase_version`, `meetings.meeting_protocol_version`, `meetings.crisis_resources_visible`, `meetings.last_activity_at`, `meetings.abandoned_at`, `callbacks.close_effect_key`, `callbacks.lifecycle_version`, `meeting_protocol_cutover`, `meeting_topic_completions`, `meeting_user_gate_outcomes`, `meeting_generation_runs`, `meeting_close_runs`, and the expanded interaction constraint in place while reverting application consumers. Compare-and-set the creation fence from `version_1` to `draining`; this stops stamped and unstamped creation while a rollback bridge deploys. That bridge must keep every existing version-1 row on the beat renderer and support new unversioned rows on the legacy renderer. After every serving instance has the bridge, transition to `legacy` and reopen unversioned creation while version-1 meetings finish or expire. Version-1 rows must never load in the legacy page. Remove the beat renderer and specialized routes only when `ended_at is null and abandoned_at is null` reaches zero for version 1. Prove one active version-1 fixture blocks removal while an abandoned fixture does not, and prove legacy creation can remain open through the cleanup period. Removing stored profile, identity, beat, cue, gate-outcome, analysis, resource, activity, abandonment, generation-claim, close-result, or cutover evidence is destructive and is never the first rollback action.

Build the additive contract, persistence, and `/next` path first. Implement the specialized route assignments and renderer on one integration branch, validate the full group through `BEAT-R` and `BEAT-S`, and promote `BEAT-L` through `BEAT-Q` together in that final PR. Do not merge active-beat-only specialized routes while the legacy page is still the production caller, and do not ship a second production sequence switch.

If empty-chair generation is unavailable, leave the active beat pending and expose the existing recoverable failure presentation. Do not write a stock sentence, an empty share, or a placeholder voice line to advance the meeting.

## Artifacts and Notes

The current control flow is:

    page phase switch -> runRound* -> choose speaker locally -> call route
    refresh -> phase switch -> run the entire round again -> append new shares

The target control flow is:

    POST /meeting/<id>/next -> claim or return one persisted active beat; atomically store a new cue before returning it
    generic page renderer -> invoke the route appropriate to that beat
    route -> persist one beat-owned share, outcome, or skip -> complete with version check
    refresh -> load stored transcript and active beat -> continue once
    refresh during cue pause -> upsert stored cue id -> wait remaining canonical delay -> acknowledge once
    refresh at spoken user outcome + active gate -> POST /user-share { beatId } -> finish canonical analysis once

The creation control flow is:

    legacy -> old app may create only unstamped meetings
    draining -> no instance may create while the dual-protocol bridge deploys
    version_1 -> new app may create only stamped meetings; existing legacy rows remain on the frozen renderer until completion or expiry
    rollback draining -> no instance may create while the symmetric bridge deploys
    legacy -> new legacy creation reopens; existing version-1 rows remain on the beat renderer until completion or expiry

The completion map is exhaustive:

    room_cue -> POST /meeting/<id>/next { completedBeatId }
    user_gate:topic -> POST /meeting/<id>/next { completedBeatId, topic }
    character_share -> POST or SSE /meeting/<id>/share with beatId
    user_gate:introduction -> POST /meeting/<id>/user-share with beatId and introText content, or beatId-only recovery after persistence
    user_gate:share -> POST /meeting/<id>/user-share with beatId and content, beatId-only recovery, or controlled pass
    crisis_support -> POST /meeting/<id>/crisis { beatId }
    generated_room_moment -> POST /meeting/<id>/room-moment { beatId }
    close_meeting -> POST /meeting/<id>/close { beatId }
    finished -> no completion request; render reflection and stop

The requirement map is: `BEAT-01` is Milestones 1 and 3; `BEAT-02` is Milestones 2 and 3; `BEAT-03` is Milestones 2 through 5; `BEAT-04` is Milestones 4 and 6; `BEAT-05` is Milestones 6 and 7; `BEAT-06` is Milestones 1, 3, 4, and 7; `BEAT-07` is Milestone 5; `BEAT-08` is Milestones 1, 3, and 6.

The March restore plan remains at `archive/plans/restore-virtual-recovery-meeting-execplan-2026-03-19.md`. Its shipped experience target and historical findings are useful evidence. Its frontend ownership rules, fixed branch instructions, old checkout paths, and prohibition on an orchestration endpoint are superseded and must not guide implementation.

## Interfaces and Dependencies

The implementation must expose a discriminated `MeetingBeat` union from `app/src/lib/core/types.ts` equivalent to:

    type MeetingBeat =
        | BeatBase & { kind: 'character_share'; characterId: string; interactionType: CharacterShareInteractionType }
        | BeatBase & { kind: 'room_cue'; cue: RoomCue }
        | BeatBase & { kind: 'generated_room_moment'; moment: 'empty_chair' }
        | BeatBase & { kind: 'user_gate'; gate: 'introduction' | 'topic' | 'share' }
        | BeatBase & { kind: 'crisis_support'; trigger: CrisisTrigger; responderCharacterId: string }
        | BeatBase & { kind: 'close_meeting' }
        | BeatBase & { kind: 'finished' };

    type CrisisTrigger =
        | { source: 'user_share'; shareId: string }
        | { source: 'meeting_intake' };

    type CharacterShareInteractionType =
        Exclude<ShareInteractionType, 'room_cue' | 'empty_chair'>;

    interface BeatBase {
        id: string;
        ordinal: number;
        phase: MeetingPhase;
        pauseAfterMs: number;
    }

`MeetingPhaseState` must add `beatCursor: number`, `activeBeat: MeetingBeat | null`, `crisisTrigger: CrisisTrigger | null`, and `intakeCrisisHandled: boolean`. Historical JSON may be revived for read-only display, including conversion of an earlier `crisisTriggerShareId` into the typed share form, but missing beat fields never create an actionable zero cursor. `nextMeetingBeat` accepts state only from a meeting whose `meetingProtocolVersion` is 1. It and `completeMeetingBeat` live in `app/src/lib/core/meeting-beats.ts` and perform no I/O. Initialization and completion require a timestamp supplied by the server clock seam; none of the pure functions or ritual helpers they call may read the system clock.

The database seam must provide `MeetingProtocolCutoverMode = 'legacy' | 'draining' | 'version_1'`, a versioned singleton read, and a service-role compare-and-set transition that permits only adjacent modes. Its meeting insert trigger must reject every insert in `draining`, unstamped inserts in `version_1`, and stamped inserts in `legacy`; `createMeeting` maps this to the stable recoverable join result. The seam also provides server-owned activity updates and bounded `expireInactiveMeetings({ protocolVersion, cutoff, now, limit })` semantics that mark only `abandonedAt`; a versioned phase read; a compare-and-set meeting-state update that can atomically include monotonic crisis-resource visibility at crisis entry; an atomic next-beat claim that includes the canonical cue row for a newly selected room cue and resources for intake crisis; beat-keyed topic-completion read/write; user-gate outcome read, spoken claim, spoken completion, and pass completion operations that contend for one key, with spoken crisis completion persisting resources before generation; `getShareByBeatId`; a terminal generation-result lookup; idempotent character persistence-record establishment; read-only callback identity resolution; versioned callback reference and lifecycle writes; generation claim/renew/token-fenced-commit/skip/fail operations for character, crisis, and room-moment beats; first-analysis-wins persistence for `UserShareAnalysis`; and claim, renew, checkpoint, lifecycle-checkpoint-refresh, atomic-finalize, and fail operations for a beat-keyed close run. The finalizer's contract fixes table order and sorted UUID locks. Exact interface names may follow existing repository naming, but their semantics and tests are fixed by this plan.

`PersistableMeetingParticipant` contains every base and narrative field and keeps `voiceExamples` as a required three-tuple. `CharacterPersistenceRecord` maps one meeting-scoped domain character id to one validated database UUID and complete canonical profile; core records resolve only through the unique immutable slug established by #79. `TopicCompletion` contains one validated topic and its completed phase version. `UserGateOutcome` is `{ kind: 'spoken'; gate: 'introduction' | 'share'; shareId: string; phaseVersionAfter: number | null } | { kind: 'pass'; gate: 'share'; shareId: null; phaseVersionAfter: number }` and contains no free text; every layer rejects an introduction pass. Generation claim results distinguish `acquired`, `in_progress`, `completed { shareId }`, and `skipped { reason: 'quality_rejected' }`; only a character run can enter the last state. The generated-share commit requires the current unexpired claim token and atomically inserts the beat-keyed share, updates participant state, attaches the share to the run, and completes that run. `ShareRecord.beatId` is nullable for history and required for new beat-owned generated, cue, and user transcript entries; `ShareRecord.roomCue` and `ShareRecord.userAnalysis` are nullable and mutually appropriate to their row kind. A spoken gate outcome plus `setUserShareAnalysisIfAbsent` and `getShareByBeatId` must support a beat-id-only route recovery that accepts no replacement content and converges with the original writer. `MeetingRecord` exposes `meetingProtocolVersion: 1 | null`, `crisisResourcesVisible: boolean`, `lastActivityAt: Date`, nullable `abandonedAt`, nullable summary, and nullable notable moments. Callback scanning exposes a pure candidate validator over the exact eligible canonical shares; it verifies meeting origin, the origin speaker, and quoted-text containment before identity resolution. `CallbackRecord` exposes `lifecycleVersion: number`; every callback mutation takes its expected version. `CallbackLifecycleTarget` contains `callbackId`, `expectedLifecycleVersion`, and the intended state. `MeetingCloseResult` validates the stored endpoint payload. The close checkpoint holds the full intended mutation set with callback targets already resolved to database UUIDs and paired with their expected lifecycle versions. The atomic finalizer accepts the current claim token and expected meeting version, deduplicates UUIDs, acquires locks in the fixed meeting/run/origin-share/character/callback order with every row collection sorted, revalidates origin relationships and lifecycle versions, and either commits meeting fields, callbacks, lifecycle targets, phase, and run result together or returns a typed conflict with no writes. A token-checked refresh operation may replace only the lifecycle targets after recomputation.

Expose one pure `deriveMeetingRoster({ meetingId, startedAt })` helper, one server-side `resolveMeetingRoster` helper, and one pure transcript-speaker helper. The server resolver is the only persisted-or-fallback path used by page load, `/next`, `/share`, `/close`, and `/expand`; it uses the pure helper when participant reads or saves cannot return canonical rows and establishes every character's persistence identity before returning a derived roster. Participant-association failure may degrade to those identity-backed seats, while identity failure stops selection with a recoverable error. The speaker helper accepts `isUserShare`, `interactionType`, and `characterId` plus trusted user and resolved-roster names; it checks user status first, maps `room_cue` and `empty_chair` to `Room`, resolves ordinary characters next, and returns `Character` for an unknown non-user entry. Close, expansion, and page hydration use the same mapping.

The next-beat route depends first on the ownership gate and `locals.meetingContext` from `plans/private-meeting-access-execplan.md`, then on epic #79's merged durable core identity slice. That slice must seed one row per core character in a migration, store the immutable domain slug in a dedicated uniquely constrained column, resolve any preexisting duplicate deterministically or stop migration, and remove lazy character creation from reads. The privacy plan's preview composition must supply the existing Grok mock as well as auth and database mocks; `BEAT-R` adds its deterministic meeting-flow script. The meeting-flow plan does not depend on a hosted provider, but its database seam must pass the real local Supabase probe before fixture, mock, adapter, or route work. Applying migrations to a hosted tenant, refreshing unrelated provider fixtures, and production verification remain blocked by #71 and #91.

Issue #90 should follow this plan for `+page.svelte`: deleting the old orchestration after the `BEAT-S` zero-active non-abandoned legacy gate will materially shrink the file and reveal the remaining honest component boundaries. Do not split the old sequence into several files before removing it. Issues #86 through #89 are independent except where their code touches a file in the active slice; do not pull them into these milestones.

## Revision Note

2026-09-12: Created this plan to replace the superseded frontend-owned March meeting plan with the server-owned direction in epic #77. The revision introduces persisted beats and idempotency before client cutover, makes replay-free refresh an acceptance condition, and gives the unused empty-chair prompt a compliant generated and persisted path.

2026-09-13: Tightened topic completion so the selected topic and phase transition are committed in one version-checked meeting-row update. This closes the race where separate writes could pair the losing topic with the winning beat state.

2026-09-14: Revised the plan after PR review to add durable generation and close claims, a terminal character-only skip, atomic close phase/result finalization, persisted room cues, deterministic roster fallback, repeated-speaker completion, bounded in-progress client retry, truthful room-entry labels, meeting-scoped callback effects, stored-summary hydration, one route-and-renderer cutover, server-derived user and expansion decisions, canonical intake/share crisis sources, crisis and empty-chair quality gates, close-summary prompt repair, deterministic Grok preview fixtures, exhaustive completion, injected time, real-probe-before-fixture ordering, and Playwright's configured test directory.

2026-09-14: Closed the next review pass by making terminal beat results authoritative after phase advancement, moving every durable close mutation into one renewable token-checked transaction, version-gating the renderer instead of guessing historical cursors, persisting sticky crisis-resource visibility, and requiring one roster resolver in every character-consuming route. Added lost-response, full-rollback, slow-holder, legacy-mid-round, and failed-save visitor acceptance cases for those decisions.

2026-09-14: Bound the trusted `createMeeting` protocol-version write to `BEAT-Q`, the same promotion boundary that activates the generic renderer. Earlier additive persistence slices deliberately leave new rows unversioned, so a meeting created by the old renderer cannot later be mistaken for a version-1 meeting; the cutover slice owns both the server stamp and the client that understands it.

2026-09-14: Added beat-keyed terminal evidence for topic and user-pass outcomes so a lost acknowledgment can return the canonical current beat after phase advancement. The later user-gate revision below replaces the pass portion with a shared spoken-or-pass outcome boundary. Narrowed `character_share.interactionType` to exclude room-owned cue and empty-chair values in both the static union and runtime validation.

2026-09-14: Added stable visitor persistence identities that survive participant-association failure, resolved callback domain ids to locked database UUIDs before close finalization, replaced separate share insertion and generation completion with one token-fenced transaction, and expanded the prompt cleanup and source audit to cover numeric and worded sentence counts such as the active crosstalk instruction. A follow-up source check made the visitor key meeting-scoped because the current visitor domain id contains only the start-time second and slot; it also changed the database mechanism to a nullable named unique constraint so Supabase upsert can target it directly.

2026-09-14: Required visitor persistence to store and rehydrate the complete immutable generated profile, especially the nonoptional three-line voice tuple, instead of reconstructing an incomplete participant from base columns. Restricted pass outcomes to later share gates so the introduction remains a real persisted share in listening-only meetings. Added a zero-active-version-1 preflight before rollback may remove the beat renderer or specialized routes.

2026-09-14: Made epic #79's unique, migration-seeded core character slug and removal of lazy core creation a hard prerequisite for `BEAT-F` and later roster work. Added callback lifecycle versions to every callback mutation and close target, atomic stale-checkpoint rejection, and a token-checked lifecycle-only recomputation path so abandoned close work cannot overwrite a callback changed by another meeting.

2026-09-14: Replaced point-in-time cutover and rollback queries with a database singleton and insert trigger that fence creation throughout both deployments. Added pure callback origin, speaker, and quote validation plus a locked finalizer recheck. Added content-free user-share recovery so refresh completes a persisted but unanalyzed share without asking the person to repeat it.

2026-09-14: Replaced independent spoken-share and pass writes with one serialized user-gate outcome contract, including both race orderings and crisis-bearing content. Moved controlled room-cue persistence into the transaction that claims and first exposes the cue beat, with refresh-during-pause recovery based on the stored cue timestamp. Renamed the planned migration to the unique Supabase version `20260912000300_server_owned_meeting_beats.sql`.

2026-09-14: Renamed the planned beat migration to `20260912000300_server_owned_meeting_beats.sql` after confirming Supabase orders migrations by the leading numeric segment, not the descriptive counter after the first underscore. The private, core-identity, and beat migrations now have distinct versions in dependency order.

2026-09-14: Closed the latest review pass by fixing finalizer lock acquisition to one table order with sorted UUIDs, replacing an indefinite zero-active drain with a temporary immutable-protocol bridge plus server-clocked 24-hour abandonment, and persisting controlled crisis resources when a crisis beat begins so provider or quality failure cannot hide the safety card.

2026-09-14: Staged `last_activity_at` through nullable add, durable backfill, database default, and non-null enforcement so the additive migration remains compatible with existing rows and legacy inserts. Required all four new meeting ledgers to cascade with parent deletion and added a real-probe deletion case. Expanded prompt repair and verification to omit empty recent-share, callback, and other optional sections entirely.

2026-09-14: Recorded isolated offline C01/C02 execution. The pure contract now rejects malformed normal and crisis-support beats, keeps room-owned interactions out of character generation, centralizes controlled crisis resources, and leaves new phase fields optional until trusted version-1 initialization. The lockfile received only the two missing optional native records needed for `npm ci`; no package version changed. This revision intentionally leaves C03 selection, all I/O, prompts, and privacy work untouched.

2026-09-14: Recorded C03 opening selection in the same isolated worktree. The engine returns only version-1-ready active state and otherwise selects Marcus's opening share from the persisted ordered roster with a cursor-derived id and the existing three-second pause. It rejects missing protocol fields before replay, uses no clock or random source, and leaves C04 room/empty-chair selection plus every I/O, prompt, and privacy change untouched.

2026-09-14: Paused before C04 after recording the unresolved opening-ritual ordering conflict. The legacy renderer, phase state machine, and share-route prompt mapping do not agree on where the empty-chair moment, moment-of-silence cue, and Chrystal reading belong. The next slice must settle that phase/cursor sequence in this plan before adding selection branches; it must not treat browser order as an implicit product decision.

2026-09-14 / PR promotion: this clean branch publishes C01/C02 beat contracts and the C03 opening-only selector for review under #77/#81. nextMeetingBeat returns a SeamResult, requires explicit version-1 initialized input, replays an existing active beat, and selects Marcus with the shipped 3000 ms pause from an ordered roster. Other phase selection and completion remain unimplemented. This is an additive pure-core checkpoint, not a route cutover or completed BEAT-A/D milestone. Privacy persists as independent draft PR #95 and remains gated on real database evidence. No Docker or hosted verification was performed.

Promotion verification: clean dependency extraction with npm ci --ignore-scripts --no-audit --no-fund succeeded (665 packages); lifecycle scripts were not tested by that install. The full server Vitest project passed 266 tests in 40 files. Targeted ESLint passed for types.ts, meeting-beats.ts and meeting-beats.spec.ts. npm run check passed with zero errors and the eight existing page warnings. The new code remains unwired; these checks do not establish database persistence or production refresh behavior.

The promotion production build also passed. This is a compilation/build result, not a deployed or live meeting verification.

2026-09-15: Addressed both PR #96 review findings with replay-context validation and persistence-type isolation. The 275 server tests and type check pass; eight existing Svelte warnings remain. No Docker, probe, route, or database changes were made. Full CI fixture freshness remains blocked under #91.
